# 테스트셋 성능 (현재 모델)

대상: `models/baseline_iforest.joblib` — IsolationForest(300 trees), 60초 윈도우 46피처(c19 제외), 건별 centering(워밍업 6시간) + 건별 임계값, 글로벌 임계값 0.5251(학습 정상 점수 99% 분위수), sustain 3, 비용접 게이트 0.5
데이터: `test/` 8파일 → `preprocessed/test/`(학습과 같은 전처리·스케일러) → 80,533 윈도우
재현: `python .py/train.py --exclude-non-welding --cv 4` → `python .py/train.py --evaluate` → `models/baseline_iforest_test_metrics.json`, `models/scores/test_N_iforest.csv`
경과(이전 모델의 수치, 절제 실험, 바뀐 이유)는 `history.md`에 있다.

## 1. 판정

**불충분.** 오탐은 설계 목표대로 잡혔지만 고장 1시간 전을 거의 못 잡고, 고장 직전 확인은 모델이 아니라 종료 코드 규칙이 한다.

| 기준 | 목표 | 현재 | 판정 |
|---|---|---|---|
| 고장으로 이어지는 지속 알람(3회 연속)이 뜨는 건 수 | 8건 중 과반 | **0/8** | ✗ |
| 그 알람의 선행 시간 | 30분 이상 | — | ✗ |
| 운영점(선행 ≥30분 & 오탐 ≤1회/일/건) 충족 건 | 과반 | 0/8 | ✗ |
| 정상 구간 알람률 | 1% 안팎 (설계) | 0.5%, 건별 0.02~1.0% (sd 0.3%) | ✓ |
| 정상 구간 지속 오탐 | 건당 하루 1회 이하 | 0~0.17회/일, 8/8건 충족 | ✓ |
| 종료 코드 규칙(모델 무관 기준선) | — | 8/8건, 선행 중앙값 9.1분 | 규칙이 모델보다 낫다 |

파이프라인이 실제로 보장하는 것: (1) 정상 구간 오탐 0.5%(건별 편차 없음), (2) 종료 코드 규칙(`rule_triggered`)에 의한 고장 직전 ~10분 critical, (3) 상시 점수와 피처 기여도.

## 2. 결과

### 2.1 전체·클래스별 (고장 전 1h vs 정상 윈도우)

| | AUROC | recall@1h | 정상 알람률 |
|---|---|---|---|
| 테스트 8건 | 0.636 | 0.029 | 0.5% |
| E01 (2건) | **0.346** | 0.000 | 0.8% |
| E02 (1건) | 0.894 | 0.100 | 0.2% |
| E03 (2건) | 0.683 | 0.025 | 0.05% |
| E04 (3건) | 0.644 | 0.028 | 0.7% |
| 검증 14건 | 0.647 | 0.030 | 1.0% (건별 0.1~4.0%, sd 1.1%) |
| 검증 4-fold (64건) | 0.631 ± 0.038 (폴드 0.59~0.73) | 0.024 ± 0.007 | 1.4 ± 0.7% |

- 에러 상태 윈도우 알람률 0.8%(모델) — 종료 상태 윈도우는 규칙이 100% 잡는다(13윈도우, 8/8건).
- 비용접 게이트로 보류된 알람 47건, 게이트 대상 윈도우 5.3%.
- 클래스당 1~3건이라 클래스별 수치는 건 하나에 좌우된다. 분할 하나의 AUROC는 ±0.04 흔들린다(4-fold).

### 2.2 파일별

| 파일 | 클래스 | 건별 임계값 | 마지막 지속 알람 (고장 전) | 지속 오탐 / 일 | 정상 알람률 | 고장 전 1h 알람률 | 비용접 비율 전체 / 고장 전 1h | 규칙 선행 |
|---|---|---|---|---|---|---|---|---|
| test_0 | E01 | 0.558 | 없음 | 0.00 | 0.6% | 0% | 12% / 0% | 9.2분 |
| test_1 | E01 | 0.528 | 없음 | 0.00 | 0.9% | 0% | 2% / 2% | 9.8분 |
| test_2 | E02 | 0.525 | 없음 | 0.00 | 0.2% | 10% | 3% / 1% | 9.1분 |
| test_3 | E04 | 0.525 | 없음 | 0.17 | 0.3% | 7% | 2% / 6% | 6.6분 |
| test_4 | E03 | 0.525 | 없음 | 0.00 | 0.1% | 0% | 0% / 0% | 9.2분 |
| test_5 | E03 | 0.525 | 없음 | 0.00 | 0.02% | 5% | 5% / 22% | 9.2분 |
| test_6 | E04 | 0.525 | 없음 | 0.17 | 1.0% | 2% | 11% / 1% | 9.1분 |
| test_7 | E04 | 0.525 | 없음 | 0.17 | 0.6% | 0% | 10% / 1% | 9.1분 |

규칙 선행의 상한은 ~10분이다(종료 코드가 그때 뜬다).

### 2.3 고장까지 남은 시간별 점수 (중앙값 / 알람률)

| 클래스 | 24h 초과 | 6~24h | 1~6h | 10분~1h | 마지막 10분 |
|---|---|---|---|---|---|
| E01 | 0.382 / 0.7% | 0.383 / 0.7% | 0.382 / 0.7% | 0.375 / 0% | 0.382 / 0% |
| E02 | 0.372 / 0.2% | 0.359 / 0% | 0.398 / 0.3% | 0.401 / 6% | 0.474 / 30% |
| E03 | 0.360 / 0% | 0.392 / 0.1% | 0.391 / 0.2% | 0.390 / 3% | 0.463 / 0% |
| E04 | 0.388 / 0.7% | 0.388 / 0.6% | 0.391 / 0.6% | 0.397 / 0.7% | 0.408 / 13% |

E01은 7일 내내 평평하다. 나머지도 **마지막 10분**에서야 오르고, 10분 이전 알람률은 어떤 클래스도 6%를 넘지 않는다.

## 3. 왜 부족한가

- **피처에 정보가 없다.** c19(시간 카운터, 레이블 누설)를 뺀 지도학습(LightGBM) 상한선이 검증 0.705 / 테스트 0.619로 비지도(0.647 / 0.636)와 같다. 정답을 알려줘도 이 윈도우 통계로는 고장 1시간 전을 가르지 못한다 — 모델 문제가 아니라 피처 문제다.
- **신호는 마지막 수십 분에만 있다.** 레이블 창을 30분으로 좁히면 AUROC가 가장 높고(검증 0.713 / 테스트 0.673) 넓힐수록 떨어진다. 고장 전 1h와 정상을 가르는 피처(효과 크기 d > 0.5)는 전부 `_std`(변동성)와 용접 활동량이고, 논문 타깃 c5·c2의 평균은 거의 움직이지 않는다.
- **모델은 종료 상태에도 약하다.** `error_code`는 입력이 아니고 `error_share_10min`은 코드가 뜬 뒤 수 분이 지나야 커진다. 이 구간은 규칙이 맡는다.
- **평가 불확실성.** 테스트 8건·검증 14건, 클래스당 1~3건. 폴드 간 AUROC 0.59~0.73.

## 4. 시도했으나 채택하지 않은 것

모두 같은 레시피(c16 ≤ 0 규칙, 비용접 제외 학습, 게이트 0.5, 건별 centering + 임계값) 위에서 검증 14건 / 테스트 8건으로 비교했다. 표와 경과는 `history.md`의 구 test.md 8절.

| 시도 | 결과 | 판단 |
|---|---|---|
| 변동성·추세 피처(5/30/60/1440분 이력), 5분 창 | 검증 AUROC 최대 0.70, 테스트 0.52~0.64. c19 제외 비교에서 +0.01~0.03(폴드 편차 안). 10분 밖 지속 알람 0건 | 채택 안 함 |
| 점수 EWMA / CUSUM / LSTM 오토인코더 | EWMA +0.01·recall 0%, CUSUM 임계값 발산, LSTM-AE 0.61 / 0.59 | 채택 안 함 — 기억할 신호가 없다 |
| E02 전용 모델 | E02 AUROC 오히려 하락(검증 0.78→0.73) | 채택 안 함 |
| 건별 정규화 `scale`(std까지 나눔) | 워밍업이 대기 상태인 건(검증 E04_4)에서 정상 알람률 56.5% | 폐기, 평균만 빼는 `center` 사용 |

## 5. 다음 단계

모델이 아니라 데이터다.

1. 새 정보원: 용접 단위 품질 신호(c11 증분마다의 파형 요약), 정비·캡 교체 이력.
2. 레이블 정의 재검토: 30분 창이 더 잘 갈라진다.
3. 워밍업 조건을 "6시간"에서 "정상 용접 N행"으로(대기 상태로 워밍업된 건 대비 — 검증 E04_4는 center에서도 정상 알람률 4.0%).
