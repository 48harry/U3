# MEMORY.md — 설계 결정 기록

이 파일은 **왜 그렇게 만들었는지**를 남기는 곳이다. 무엇이 어디에 있는지는 `readme.md` 5절, 코드 동작은 각 스크립트 docstring을 본다.
결정이 바뀌면 해당 항목을 고치고 맨 아래 변경 이력에 한 줄 남긴다. 코드에서 읽을 수 있는 내용은 적지 않는다.

## 1. 프로젝트 목표

- 데이터: RSW 용접기 고장 예측 벤치마크(Wang et al., Sci Data 2024, `docs/RSW Paper.pdf`). train 72파일(E01~E04 × 18), test 8파일, 1 Hz × 7일.
- 논문의 2-stage(센서 예측 → 진단 규칙)는 오류 누적으로 정확도 68%/오알람 56% → **우리는 1-step 이상탐지**로 간다 (readme 2절).
- 최종 파이프라인: 이상탐지 시그널 → 온톨로지/RAG 원인 분석(E01~E04 매뉴얼) → LLM 리포트 → Slack (readme 3절). 지금은 이상탐지 API까지 구현.

## 2. 데이터에 대해 확정된 사실 (EDA, 72파일)

- **`error` 컬럼은 이벤트가 아니라 상태 코드**다. 같은 코드가 수 시간 지속(E003 중앙값 1.5~2h, 최장 28h). 샘플 수로 세면 안 되고 에피소드(연속 구간)로 센다. gap이 에피소드를 끊지 않도록 코드를 gap 너머로 carry한다.
- **클래스별 종료 코드는 거의 결정적**: E01→E012, E02→E016, E03→E028, E04→E029 (18/18, 17/18, 18/18, 18/18), 모두 고장 전 10분 안에만 등장. 파일 마지막 행의 error는 항상 `0`이라 "마지막 행"이 아니라 "마지막 수 분"을 봐야 한다.
- E003·E011·E029는 며칠 전부터 장시간 지속되므로 단독 전조로는 약하다. readme의 "잔고장 빈도 = 전조" 가설은 부분적으로만 성립.
- 결측은 행 단위 block-out. E01/03/04는 ~1%, gap 99%가 ≤35초. **E02 8개 파일(0,2,3,4,5,6,7,8)은 64~84%** → 논문 규칙(40%)으로 폐기. E02_2는 10분짜리 데모 파일.
- c16(판 두께 setpoint) **≤0 = 캡 드레싱 등 비용접 작업**, 파일당 0.1~44%(평균 10%). 학습 파일에서 `c16 > 5`는 행의 1.3% 미만이지만 그 행들은 시간당 800~980회 용접한다(얇은 판 구간 520~600회보다 빠름) — **`>5`는 두꺼운 판 setpoint이지 비용접이 아니다.** 학습 파일의 실제 비용접(카운터 정지)은 사실상 전부 `c16 == 0`. 벤치마크 코드의 `>5` 분기를 그대로 따랐던 2026-09-23 이전 규칙은 test_2·test_3(행의 85~92%가 `c16 = 8.7`)의 87~95%를 비용접으로 오분류했다. 단 그 두 건의 8.7 구간은 시간당 27~72회로 대부분 대기 상태다 — 저활동은 `welds_10min`·`weld_duty_10min`이 표현하고, `non_welding`은 setpoint 상태만 뜻한다.
- c7·c8·c9는 파일 내 상수(건 고유 속성). c11·c12는 누적 카운터라 절대값이 건마다 다르다. c4·c18은 거의 이진, c17 거의 상수.
- 시각은 CSV의 `...Z`를 **naive UTC**로 통일한다(`pd.to_datetime(utc=True).tz_localize(None)`). 로컬(KST) 변환 금지 — `hour_sin/cos`가 틀어진다.
- E02 일부는 2019-12~2020-07, 나머지는 2021-08~10 수집. 시기 차이가 분포 차이를 만들 수 있음(미검증).
- 논문 타깃: E02는 c2(전극 압력), 나머지는 c5(밸런스 압력). 원본 벤치마크 코드는 이와 달리 c1~c5를 타깃으로 씀.
- **c19("offset value in robot")는 센서가 아니라 초당 ~55씩 단조 증가하는 카운터**(경과 시간과 상관 1.000). 모든 파일이 정확히 7일이고 고장에서 끝나므로 건별 centering 후 c19 = 168h − 잔여 시간 = **레이블 누설**. 지도학습이 c19로 AUROC 0.98, 없이 0.62. `train.py` 기본 `--drop-features c19`(번들 `dropped_features`). 2026-09-23 발견(`history.md` 구 test.md 8.1절). 2026-09-26 재점검: 다른 피처의 건 안 경과 시간 상관은 |ρ| ≤ 0.32로 추가 누설 없음.
- **"고장" = 종료 코드 + 10분.** 적중 트리거 72건의 선행이 중앙값 10.0분, IQR 10.0~10.0(2026-09-26). 규칙 선행 ~10분은 상한이자 데이터셋의 정의다. 그래서 짧은 레이블 창의 AUROC 이득은 전부 이 마지막 10분(종료 상태)에서 나온다.
- **`_std` 피처는 프로그램 전환 지표다.** c2~c5·c13·c18의 `_std`는 `c16_std`(판 두께 설정 전환)와 건 안 경과 시간 상관은 |ρ| ≤ 0.32로 0.8~0.9. 현재 데이터에서 고장 전 1h 효과 크기 < 0.05 — `history.md` 3.1절의 "흔들림이 전조"(d 0.57)는 구 c16 규칙 시절 값이라 성립하지 않는다.

## 3. 전처리 결정 (`.py/preprocess.py`)

| 결정 | 이유 |
|---|---|
| 결측률 >40% 파일 폐기 | 논문 규칙. 80%대 파일은 ffill해도 상수 덩어리 |
| gap ≤60초 ffill, 초과 gap은 행 삭제 + `segment_id` 분리. 구현은 `preprocess.fill_gaps` 하나이고 `main.py`도 이것을 import한다 | gap 99%가 ≤35초. 윈도우가 구멍을 넘지 않게. **2026-09-22판은 `ffill(limit).bfill(limit)`이라 실제로는 120초까지 메꿨다**(학습 15개 파일·test_5에 해당 gap 존재) — 합성 데이터 스모크 테스트가 잡아냄. 같은 로직을 두 곳에 복제하지 말 것 |
| 비용접 = `c16 <= 0`만(`--outlier-hi` 기본 None; `--outlier-hi 5`로 논문·벤치마크 규칙 복원) | §2 c16 항목. `>5` 분기는 두꺼운 판 용접을 비용접으로 오분류해 test_2·test_3에서 오탐의 전부를 만들었다 |
| 비용접 행은 센서값 blank → 직전 용접값 carry(길이 제한 없음), 행은 유지 + `non_welding` 플래그 | 논문 방식. 60초 제한을 걸었더니 고장 직전 1시간이 날아가는 파일(E01_0)이 있었음 |
| c11·c12 → `welds_delta`, `pos_delta`, `welds_10min` | 건 간 비교 가능한 값으로 |
| `error_active`, `error_share_10min`, `terminal_code` 파생 | 상태 코드를 피처와 레이블 양쪽으로 쓰기 위해 |
| `ttf_s` + `label`(마지막 3600초=1) | 지도/비지도 양쪽에서 쓸 수 있게. 윈도우 길이는 옵션 |
| z-score를 **정상행(`label==0 & error_active==0`)** 으로만 fit, `scaler.json` 저장 | 비지도 학습 관례. 테스트/서빙에 동일 변환 적용 |
| `error_share_10min`, `weld_duty_10min`은 스케일링 제외 | 이미 0~1 비율. 정상행 기준 z-score가 30 같은 값을 만들었음 |
| 기본은 global 스케일, `--scale per-file`·`--drop-static` 옵션 | 건 간 오프셋 제거는 실험 대상 |
| 테스트 8파일은 `--split test`로 **같은 코드 경로**를 타되, 옵션은 train의 `preprocess_config.json`, 스케일러는 `scaler.json`을 읽어서 적용(재적합 금지). 출력은 `preprocessed/test/` | 학습/테스트 변환이 한 글자라도 다르면 평가가 무의미. train 산출물(manifest, scaler)을 덮어쓰지 않으려고 하위 폴더 |
| 테스트 파일 클래스는 **마지막 10분의 종료 코드**로 추론(`class_source`에 근거 기록). 여러 코드가 있으면 고장에 가장 가까운 것 | 파일명(`test_N`)에 클래스가 없다. 2절의 종료 코드 매핑이 71/72로 결정적이라 신뢰 가능. test_3은 E016→E029 순이라 E04 |

## 4. 모델링 결정 (`.py/train.py`)

- 60초 윈도우 집계(연속 피처 mean+std = 46 피처; c19 제외 전에는 48). 37M 초 단위 행을 그대로 쓰지 않는다 — 메모리와 시간 문제, 그리고 윈도우 통계가 시간 맥락을 준다.
- **P1 실험 결론(2026-09-23, `history.md`(구 test.md 8절), `.py/experiments.py`, `results/p1p2/`)**: 이력·추세 피처, 5분 창, 점수 EWMA/CUSUM, LSTM-AE, E02 전용 모델 — 전부 채택 안 함. 정직한 지도학습 상한선(c19 제외)이 검증 0.705 / 테스트 0.619로 비지도(0.647/0.636)와 같다 → **이 윈도우 통계에는 고장 1시간 전 정보가 없다(피처 문제, 모델 문제 아님)**. 운영점(선행 30분·오탐 1/일) 충족 테스트 건 0. **P3(2026-09-26, `history.md` 11절, `results/p3/`)**: 설정값 조건부 잔차, 정비 이력 피처(캡 드레싱·c1 변경, 6h 상한), 짧은 레이블 창 — 전부 채택 안 함(정비 피처는 IF 4-fold +0.03·테스트 −0.05로 매번 반대 방향 = 과적합). 데이터셋 안에서 만들 수 있는 정보는 소진 — 고장 10분 이전 분리력 AUROC ~0.6이 천장. 다음은 데이터셋 밖(용접 1타 파형, 실제 정비 기록).
- **평가 설계(P2-2)**: `evaluate()`가 운영점(`operating_point`: 선행 ≥30분 & 오탐 ≤1/일/건 충족 건 수)과 종료 코드 규칙 기준선(`rule_terminal_code`)을 항상 같이 낸다. 규칙 기준선은 **서빙과 같은 방식**(4개 코드 전부 = 윈도우 플래그 `terminal_any`, 에피소드 시작 시 1회, `RULE_COOLDOWN_S` 30분 쿨다운, `train.rule_triggers`)으로 적중(고장 전 1h 안 첫 트리거의 선행)과 오트리거/일을 낸다: 테스트 8/8건·9.2분·0.25회/일, 검증 14/14·9.4분·0.05회/일. 2026-09-24 전에는 파일 자기 클래스의 코드만 셌다(`terminal_code`) — 서빙은 클래스를 모르므로 다른 클래스 코드의 오트리거가 평가에서 빠져 있었다. `--cv k` 건 단위 층화 k-fold(폴드 간 AUROC 0.59~0.73, 한 분할 수치는 ±0.04 흔들림 — 구성 비교는 4-fold로), `--label-window`로 레이블 창 변경. **레이블 창은 1시간 유지**(2026-09-26): 좁히면 AUROC가 오르지만(IF 4-fold 15분 0.78 / 30분 0.68 / 60분 0.63) 고장까지 남은 시간 구간별로는 0~10분만 0.77~0.84, 10분 밖은 0.59~0.67 — 이득이 규칙이 덮는 구간에서만 나온다. 대신 **AUROC를 세 가지로 본다**: 풀링 `auroc`, 건별 평균 `auroc_gun_mean`(건 간 점수 오프셋 제거; 테스트 건별 0.24~0.89), 규칙 이전 `auroc_pre_rule`(고장 10분 이전 전조만 양성 = 모델이 규칙에 더하는 몫, `RULE_LEAD_S`). 규칙 기준선의 오트리거는 **critical 발화**(반복 제외) 기준이고 `all_triggers_false_per_day_mean`이 반복까지 센 값.
- **분할은 파일(=건) 단위**, 클래스별 층화. 같은 건의 행이 train/val에 섞이면 안 됨.
- 정상 = `label==0 & error_active==0`. **기본 학습은 `--exclude-non-welding`**(비용접 비율 > 0 윈도우 제외, 464k → 418k 윈도우). 성능 장치가 아니라 "비용접 중에는 판단하지 않는다"는 운영 규칙이다.
- **비용접 게이트**: 윈도우의 비용접 비율이 `--alarm-max-non-welding`(기본 0.5, `none`으로 해제)을 넘으면 점수가 임계값을 넘어도 알람이 아니다(보류). 평가와 서빙이 같은 규칙을 쓰도록 번들 `alarm_max_non_welding`에 넣는다. 지표에 `held_windows`(보류된 알람 수), `held_share_of_windows`(게이트 대상 윈도우 비율), `auroc_welding_windows`가 같이 나온다. 게이트가 사실상 두 건 전체를 가리는 구성(구 규칙 + 게이트: test_2·test_3의 99.8%)은 채택하지 않았다 — 오탐률은 0이 되지만 모델이 그 건을 보지 않는 것이다.
- **건별 정규화(P0-2, 2026-09-23)**: 건마다 센서 오프셋이 최대 1.7σ라 글로벌 z-score + 글로벌 임계값은 건별 정상 알람률을 0~13%로 흩어 놓는다. `--scale per-file`은 서빙이 재현 못 하므로 **워밍업 방식**: 파일/스트림 첫 6시간의 정상·용접 행(600행 이상) 평균을 이후 행의 z-score 입력 19개에서 뺀다(`--gun-norm center`, c7~c9는 0이 됨). 워밍업 구간은 글로벌 스케일로 점수화하고 `warmup` 플래그. 구현은 `train.window_file()`(오프라인)과 `main.Detector.gun_normalise()`(온라인) 한 쌍이고 레시피는 번들 `gun_norm`.
- **건별 임계값** = max(글로벌, 워밍업 윈도우를 건별 통계로 재정규화한 점수의 99% 분위수). 기본 켜짐(`--no-gun-threshold`). 올리는 방향으로만 쓴다 — 360윈도우의 분위수는 거칠다.
- **`--gun-norm scale`(std까지 나눔)은 쓰지 말 것.** 워밍업 동안 대기 상태였던 건(검증 E04_4)은 std가 하한에 걸려 이후 정상 알람률 56.5%. 워밍업이 정상 운전을 대표한다는 보장이 없으면 평균만 빼는 것이 안전하다. 같은 이유로 E04_4는 center에서도 4.9%로 남는다(워밍업 조건을 "정상 용접 N행"으로 바꾸는 것이 후보).
- 임계값 = 학습 정상 점수 99% 분위수(=오탐률 1% 설계). `sustain=3` 연속 초과를 "지속 알람"으로 정의 — readme 3절의 "연속 N회일 때만 RAG 트리거"에 대응.
- 평가는 AUROC/AUPRC(고장 전 1h vs 정상) + 정상 알람률 + 클래스별 + 파일별 "고장으로 이어지는 마지막 알람 구간 시작 시각"과 "24h 이전 오탐 에피소드/일". "최초 알람 시각"은 초반 오탐에 걸려 무의미해서 버렸다.
- 워밍업 윈도우(글로벌 스케일)는 학습 정상 집합에 **일부러 남긴다**. 서빙도 워밍업 구간을 같은 모델·글로벌 스케일로 판정하기 때문 — 빼면 AUROC는 같고 정상 알람률이 3배(검증 1.0→3.2%, 테스트 0.5→1.5%, 2026-09-26).
- 번들(joblib dict)에 `feature_reference`(정상 윈도우 중앙값)를 넣는다 — 서빙에서 피처 기여도 계산용.
- 베이스라인(IsolationForest, 2026-09-23 c19 제거 후 재학습, 46피처, 글로벌 임계값 0.5251) 검증 결과: AUROC 0.647, recall@1h 3.0%, 정상 알람률 1.0%, 건별 0.1~4.0%; 4-fold AUROC 0.631 ± 0.038. (c19 포함 P0-2 시점 0.684 / 3.9% / 1.1%; P0-1 후 0.642; P0-1 전 0.630.) **기준선일 뿐이고 종료 상태(E029 등)는 규칙이 잡는다.**
- 번들에는 `scaler.json`과 `preprocess_config.json`을 같이 넣는다. 서빙이 전처리 파라미터(gap 한도, c16 임계)를 번들에서 읽어야 학습·서빙이 어긋나지 않는다.
- 테스트 8파일 평가는 검증과 **같은 함수, 같은 임계값**(학습 정상 99% 분위수)으로 한다. 학습 직후 `metrics["test"]`에 들어가고, `--evaluate`는 저장된 번들만 읽어 `models/baseline_<model>_test_metrics.json`과 `models/scores/test_N_<model>.csv`를 쓴다(번들은 건드리지 않음). 테스트 클래스별 수치는 "추론된 클래스 기준"이다.
- 베이스라인 테스트 결과(8파일, 80,533 윈도우, c19 제거 후): AUROC 0.636, recall@1h 2.9%, 정상 알람률 0.5%, **건별 0.02~1.0%(sd 0.3%)**. 클래스별 AUROC E01 0.346 / E02 0.894 / E03 0.683 / E04 0.644. 고장으로 이어지는 지속 알람 0/8건, 운영점 충족 0/8. (c19 포함 P0-2 시점 0.639 / 2.9% / 0.5%.) **P0-1 후(0.679 / 4.6% / 0.7% / 건별 0~1.7%)보다 AUROC가 내려간 것은 이전 수치의 일부가 건 오프셋이었기 때문** — 0.639가 지금 피처·모델의 진짜 자리. P0-1 전은 0.687 / 5.4% / 3.2% / 건별 0~13.2%. 절제 실험 표는 `history.md`(구 test.md 6·7절), 지표 JSON은 `results/p0/`.
- **테스트 판정(`history.md` 구 test.md 1절, 2026-09-23): 불충분.** 고장으로 이어지는 지속 알람은 8건 중 1건(1.6분 전)뿐이고, 종료 코드 규칙(8/8건, 7.5~10분 전)이 모델을 이긴다. 원인 순서: ① 비용접 구간 인공물(test_2·test_3이 오탐의 전부, 고장 전 1h의 72%가 비용접) ② 건 간 오프셋 + 글로벌 임계값(건별 정상 알람률 0~13%) ③ 1h 전 갈라지는 피처가 48개 중 9개, 전부 `_std`(값이 아니라 변동성) ④ 모델에 시간 기억 없음. **모델을 바꾸기 전에 ①②부터** — 지금 바꾸면 인공물을 같이 최적화한다. **① 완료(2026-09-23, `history.md`(구 test.md 6절))**: 원인은 `c16 > 5` 규칙이었고, 수정 후 정상 알람률 0.7%·E02 AUROC 0.86. **② 완료(`history.md`(구 test.md 7절))**: 워밍업 기반 건별 centering + 건별 임계값으로 건별 정상 알람률 편차 sd 0.3%(테스트). 판정은 여전히 불충분(지속 알람 0/8건, 마지막 10분 이전 알람률 ≤6%). **P1·P2 전부 실행(`history.md`(구 test.md 8절))**: 채택된 모델 변경 없음, c19 누설 제거, 규칙·평가 설계 구현. 결론은 피처(정보) 부족 — 다음은 새 정보원.

## 5. 서빙 계약 (`main.py`)

- 입력은 CSV 컬럼 그대로(`time, c1~c19, error`). 서버가 건별 30분 링버퍼를 유지하고 전처리를 온라인으로 재현한다. 클라이언트는 아무 전처리도 하지 않는다.
- 출력 `AnomalyResult` 필드는 온톨로지 단계와의 **계약**이다: `is_anomaly, anomaly_score, threshold, score_z, severity(normal|warning|critical), sustained_alarm, consecutive_alarms, contributing_features[], context{latest_error_code, known_code_class_hint, ...}, model{}`. 필드를 빼거나 의미를 바꾸면 하류가 깨진다 — 추가는 자유.
- `severity: critical` = 임계 초과(게이트 통과)가 **`sustain × window` 초(180초) 이상 연속**(`alarm_duration_s`, 알람 시작 시각 기준). 이게 RAG 트리거 기준. 요청 횟수로 세면 호출 주기에 따라 3초~75분으로 바뀌었다(2026-09-24 수정). 청크 안의 완결 윈도우(최신 행부터 window 간격)를 모두 스코어링해 알람 상태를 윈도우마다 갱신하고, 청크 중간에 critical이 있었으면 `critical_in_request`/`sustained_in_request` — 하류 트리거는 `critical_in_request`를 본다.
- `contributing_features`는 모델 무관 perturbation(피처 하나를 정상 기준값으로 치환했을 때의 점수 감소). 모델을 바꿔도 이 방식은 유지 가능.
- `known_code_class_hint`는 모델과 무관하게 2절의 종료 코드 매핑으로 채운다. 모델이 약할 때 온톨로지가 쓸 수 있는 안전장치. **P2-1(2026-09-23)**: 그 규칙이 뜨면 `severity=critical` 강제, `rule_triggered=true`, `severity_source`(model/rule/model+rule/none). 모델이 종료 상태를 임계값 아래로 보는 문제(에러 상태 알람률 ~1%)를 규칙으로 메운다. 규칙이 보장하는 선행은 마지막 ~10분(코드가 그때 뜬다). **2026-09-24 수정: 상태가 아니라 에피소드 시작에서 1회 발화 + 건별 쿨다운 30분**(번들 `rule`). 코드가 떠 있는 동안 계속 critical이면 E029가 다른 클래스 건에서 수 시간(테스트 test_0 16.2h·test_4 9.4h, EDA 최장 24h) critical을 냈다. `rule_code`/`rule_class_hint`는 발화시킨 코드(청크 끝에서는 코드가 이미 0인 경우가 대부분이라 `known_code_class_hint`로는 클래스가 사라진다). `rule_triggered`의 의미를 바꾼 것은 이 계약 변경의 예외 — 상태 의미는 `rule_code_active`로 남겼다. **반복 강등(2026-09-26)**: 같은 코드가 같은 건에서 `rule.repeat_s`(24h) 안에 이미 발화했으면 `rule_triggered=true` + **`rule_repeat=true`, severity `warning`, `critical_in_request=false`, 핸드오프 없음**. 80건에서 반복 발화는 18회 중 17회가 오트리거(적중 손실 1/72). 테스트 critical 오트리거 0.25→0.18회/일/건. 발화 시점 특징(코드, 주변 에러 코드, 점수, 센서)으로 진짜/오트리거를 가르는 분류기는 건 단위 CV AUROC 0.62로 버렸다. 구현은 `train.rule_repeats()` / `main.Detector.rule_check()` 한 쌍(쿨다운과 같은 구조), 윈도우의 `terminal_idx`가 코드를 나른다.
- 워밍업(60초 미만)은 202 + `WarmingUp` 스키마.
- 서빙의 건별 정규화(P0-2): 건마다 첫 6시간(번들 `gun_norm.warmup_s`)의 행을 모아 두었다가 워밍업이 끝나면 건 평균과 건별 임계값을 고정한다. 그 전 응답은 `gun_norm: warming_up`(글로벌 기준), 이후는 `gun`; 정상·용접 행이 600개 미만이면 `global`. 응답의 `threshold`는 **그 윈도우를 판정한 값**(건별 있으면 건별), `model.threshold`는 글로벌. 요청당 최대 `MAX_CHUNK` = 1,200행(버퍼 30분 − 롤링 이력 10분)이고 넘으면 422 — 예전에는 3,600행까지 받아 버퍼를 넘는 행을 조용히 버렸다(50분 청크 → 워밍업 21,600행 중 13,688행만 수집). `DELETE /guns/{id}`가 워밍업을 재시작한다. test_0 실측: 25분 청크로 6.5시간 보내면 점수 동일, 건별 임계값 차이 0.0002.
- 서빙은 게이트도 번들(`alarm_max_non_welding`)에서 읽는다. 보류 윈도우는 `is_anomaly`는 그대로 두고 `alarm_held=true`·`hold_reason`을 붙이며 `severity=normal`, `consecutive_alarms`를 0으로 리셋한다(비용접 중 점수는 신뢰하지 않으므로 지속 알람 카운트를 이어가지 않는다). `AnomalyResult`에는 필드 추가만 했다.
- 서빙은 번들의 `preprocess_config`에서 gap 한도·c16 임계를 읽고(없으면 기본값 + 경고; `outlier_hi`는 None 허용), `per-file` 스케일이나 `--resample`로 만든 번들은 기동 시 거부한다 — 온라인으로 재현할 수 없는 변환이라 조용히 틀린 점수를 내느니 실패하는 쪽을 택했다.
- 비용접 행은 **건별로 들고 있는 마지막 용접 값**(`GunState.carry`)을 carry한다. 버퍼 안에서만 ffill하면 30분보다 긴 캡 드레싱/대기 구간에서 버퍼 전체가 NaN → HTTP 500(실데이터 test_0 재생에서 발견, 2026-09-24). 한 번도 용접 행을 못 본 스트림은 202.
- 온라인 전처리가 오프라인과 같은 점수를 내는지는 `tests/test_pipeline.py`(합성)와 실데이터(test_3 마지막 30분, 점수 일치)로 확인했다. 요청당 ~40 ms.

- **ML → RAG 핸드오프(2026-09-25, `.py/rag_mapping.py`, readme 5.10)**: 역할 분담 결정 — ML은 ① 이상 감지와 ② 증상 번역(기여 피처 → 방향 → 증상 P1~P9 → 고장 유형 → **상황 ID S01~S10**)까지만 하고, 원인·점검·매뉴얼(③)은 온톨로지(지현), 리포트(④)는 RAG가 맡는다. 핸드오프에 부품·점검·매뉴얼을 넣지 않는다(테스트로 고정). 상황 ID는 팀 문서 「RSW 용접건 MVP 오류 상황 정의서」의 키. 트리거는 critical 에피소드 첫 요청 + 규칙 발화마다(반복 발화 제외; 모델 critical과 겹친 반복 발화는 핸드오프의 규칙 근거 — `trigger.rule_code`, `fault_class.basis=rule_trigger` — 로 쓰지 않는다. `GunState.was_critical`, `replay.py`와 같은 규칙). `/predict` 응답에 `handoff` 필드 **추가**(평소 null), 메모리 outbox → `GET /handoffs`(pull), `RSW_RAG_URL`이면 백그라운드 POST(push, 재시도 없음), `POST /handoffs/preview`. 스키마 `RagHandoff` v1.0. 신뢰도 low/medium뿐. 실데이터 재생(테스트 8건 마지막 12h): 9 이벤트 중 8건이 고장 직전 올바른 상황을 맨 앞에, 센서 증상이 매뉴얼 패턴과 맞은 건 1건 — 믿을 키는 `situation_ids[0]`(고장 유형 유래).

## 6. 코드 규약

- 스크립트는 `.py/`, API는 루트 `main.py`(`uvicorn main:app`). `main.py`는 `sys.path`에 `.py/`를 넣어 `train`, `preprocess`를 import한다 — 번들 언피클에 `train.PCADetector`가 필요하다.
- 모든 경로는 `PROJECT_ROOT = __file__ 기준` 으로 잡는다. cwd 의존 금지 (IDE에서 `.py/`가 cwd가 되어 KeyError가 났던 적 있음). 파일을 못 찾으면 즉시 `SystemExit`로 경로를 찍는다.
- 수치는 float32, 저장은 parquet(pyarrow 없으면 csv 폴백).
- 데이터 폴더(`train/`, `preprocessed/`) 안에 산출물을 쓰지 않는다. 결과는 `eda_out/`, `models/`, `results/`로.
- 그래프 색은 클래스 고정: E01 `#2a78d6`, E02 `#eb6834`, E03 `#1baf7a`, E04 `#eda100`. 이중 축 금지.
- 환경: Windows, CPU-only torch, darts 0.41, pandas 2.3, sklearn 1.7, fastapi 0.121, pydantic 2.13. `accelerator="gpu"` 하드코딩 금지.
- 무거운 작업(72파일 EDA 40분, 벤치마크 학습 수십 시간)은 백그라운드로 여러 개 띄우지 않는다 — 메모리 부족으로 강제 종료된 적 있음. `--max-files`/`--pattern`으로 축소 테스트 먼저. 참고: 전처리 72파일 ~7분, `train.py` 전체 ~1분.
- `pytest`(= `tests/test_pipeline.py`)는 합성 CSV로 전처리→테스트 분할→학습→평가→API까지 임시 폴더에서 돌린다(~1분). API 스트림은 `.py/replay.py`(원본 CSV → `/predict`)로 재현하고, 테스트도 `replay_file()`을 TestClient로 돌린다. 파이프라인 코드를 고치면 먼저 이걸 돌린다. 실데이터 폴더는 건드리지 않는다.
- **문서 역할 분리**(2026-09-24, 사용자 지시): `readme.md`·`test.md`는 **항상 최신 상태·결과만** 담는다(이전 수치, "〔수정〕 처음엔 …" 같은 경위, 날짜별 진행 표를 두지 않는다). 수정 기록·이유·절제 실험·이전 모델 수치는 `history.md`에 시간순으로 쌓는다. 실험이나 변경을 하면 history.md에 절을 추가하고 readme·test.md는 결과를 덮어쓴다. `techspec.md`(2026-09-26 추가)는 **앞으로 개발할 것**만(ML 파트 + RAG 핸드오프 범위) — 항목을 끝내면 거기서 지우고 결과를 readme·test.md·history.md에 반영한다.
- 린트/포맷 설정은 `pyproject.toml`(줄 120자, `Benchmark.py`·`pic code/`는 원본이라 제외). 의존성은 `requirements.txt`. `__pycache__`는 git에서 뺐다.

## 7. 미결 사항 / 알려진 함정

1. E01_0은 고장 직전 1시간의 85%가 `non_welding`. 72파일에서 같은 패턴이면 `non_welding`이 레이블 누설이 되므로 입력에서 제외해야 한다.
2. `Benchmark_fixed.py`는 임포트·경로만 검증됨. 실제 학습 경로 미검증.
3. 서빙은 트레일링 60초, 학습은 분 경계 정렬 윈도우. 차이는 작지만 존재.
4. ~~윈도우 확대, 롤링 std·추세 피처, 시퀀스 오토인코더, 지도학습 상한선~~ 전부 실행됨(`history.md`(구 test.md 8절), 채택 없음). ~~정비 이력 피처, 설정값 잔차, 레이블 창 재정의~~ P3에서 실행, 채택 없음(`history.md` 11절). 남은 후보: **데이터셋 밖 정보원**(용접 1타 파형, 실제 정비 기록), E02 수집 시기 분포 차이 확인, 워밍업 조건을 "6시간"에서 "정상 용접 N행"으로(E04_4 같은 대기 건 대비).
9. 규칙 쿨다운은 코드를 가리지 않는다 — test_3은 E016→E029 순이라 규칙이 E016(E02)으로 발화하고 E029는 눌린다(오프라인 추론 클래스는 E04). 코드별 쿨다운이 후보. 오트리거(다른 클래스의 E029 등)는 반복 강등 후 critical 기준 테스트 0.18회/일/건(test_0 0.72가 최대). 남은 critical 오트리거는 전부 첫 발화이자 다른 클래스 코드 — 건의 고장 유형 정보가 있으면 거를 수 있다.
10. 건별 서빙 상태(버퍼·워밍업·쿨다운·반복 이력·carry)는 메모리에만 있다. 재시작 = 6시간 워밍업 재시작. 하류(RAG/LLM/Slack) 미구현.
8. 검증 4-fold의 폴드 간 AUROC가 0.59~0.73 — 클래스당 1~3건인 한 분할의 수치는 ±0.04 흔들린다. 두 구성의 차이가 그 안이면 차이가 아니다.
5. ~~test_2·test_3의 87~95%가 `non_welding`~~ 해소(2026-09-23): `c16 > 5` 규칙 오분류였다. 새 규칙에서 2.5%·2.1%. 대신 `c16 = 8.7` 구간이 z-score 입력으로 들어가므로 판 두께가 다른 건에서 오프셋(미결 4·P0-2)으로 나타날 수 있다.
6. ~~P0-1 후 E01 두 건의 정상 알람률 1.6~1.7%~~ P0-2로 해소(0.5·0.8%, 건별 임계값 0.577·0.530).
7. 워밍업 6시간이 대기 상태인 건(E04_4)은 center에서도 정상 알람률 4.9%로 남고, scale이면 56.5%로 폭주한다. 새 건 투입 후 첫 6시간은 글로벌 기준으로만 판정된다.

## 변경 이력

- 2026-09-22: 최초 작성. Benchmark 리뷰/수정, EDA, 전처리, IsolationForest 베이스라인, FastAPI 서빙까지의 결정 정리.
- 2026-09-23: 테스트 8파일 전처리(`--split test`, train 설정·스케일러 재사용, 종료 코드로 클래스 추론)와 테스트 평가(`train.py` 학습 후 자동 + `--evaluate`) 추가. §3·§4·§7-6 갱신.
- 2026-09-23: 파이프라인 점검. gap 채움 버그(ffill+bfill → 120초) 수정 후 전처리·재학습, 번들에 `preprocess_config` 포함, 서빙이 그것을 읽도록 변경, 합성 데이터 E2E 테스트(`tests/`)·`requirements.txt`·`pyproject.toml` 추가. §3·§4·§5·§6·§7 갱신(미결 1번 해소).
- 2026-09-23: `readme.md` 0~4절 재정리(사실 오류 수정: error 코드는 11종·원본 4,243만 행·결측 40% 초과 E02 8개, 파일명 표기 `test_0.csv`/`E01_0.csv`, c9 Start friction, c16 임계는 논문 6이 아닌 코드 5). 초기 구상과 EDA 후 바뀐 결정을 〔수정〕 표시로 구분.
- 2026-09-23: `readme.md`에 파생 피처·플래그 상세(5.4.1), 윈도우 처리 상세(5.6.1), EDA 결과 상세(5.3.1~5.3.7) 추가.
- 2026-09-23: `test.md` 작성 — 테스트셋 성능 판정, 원인 진단(효과 크기·구간별 점수·비용접 교란·건별 오프셋), P0~P2 개선 목록. §4에 요약 추가.
- 2026-09-23: **P0-1 비용접 구간 처리**(`history.md`(구 test.md 6절)). c16 확인 결과 `>5` 분기는 두꺼운 판 용접이라 규칙을 `c16 <= 0`만으로 변경(`--outlier-hi` 기본 None), 전처리 2·3단계 재실행. 학습 기본을 `--exclude-non-welding`으로, 평가·서빙에 비용접 게이트(`--alarm-max-non-welding` 0.5, 번들 `alarm_max_non_welding`, API `alarm_held`/`hold_reason`) 추가. 테스트 정상 알람률 3.2%→0.7%, test_2·test_3 지속 오탐 8.3·7.2→0/일, E02 AUROC 0.80→0.86. 판정은 여전히 불충분. §2·§3·§4·§6·§7 갱신(미결 5 해소, 6 추가).
- 2026-09-23: **P0-2 건별 정규화·임계값**(`history.md`(구 test.md 7절)). 워밍업(첫 6시간 정상·용접 행) 평균을 빼는 `--gun-norm center`(기본)와 건별 임계값(max(글로벌, 워밍업 99% 분위수), 기본 켜짐)을 `train.py`·`main.py`에 같은 절차로 구현, 번들 `gun_norm`, API `gun_norm`/`gun_threshold` 추가. 절제: center / center+임계값 / scale / scale+임계값 → center+임계값 채택, scale 폐기(검증 E04_4 56.5%). 테스트 건별 정상 알람률 0~1.7% → 0.03~0.9%, AUROC 0.679 → 0.639(오프셋이 만들던 몫). §4·§6·§7 갱신(미결 4 수정, 6 해소, 7 추가).
- 2026-09-23: **P1·P2 전부 실행**(`history.md`(구 test.md 8절), `.py/experiments.py`, `results/p1p2/`). **c19가 시간 카운터(레이블 누설)임을 발견** → `train.py` 기본 `--drop-features c19`, 운영 모델 46피처로 재학습. 이력·추세 피처, 5분 창, EWMA/CUSUM, LSTM-AE, E02 전용 모델 채택 안 함. 정직한 지도학습 상한선 = 비지도 수준(피처 문제). P2-1 종료 코드 규칙 → `main.py` `rule_triggered`/`severity_source`, `evaluate()` 규칙 기준선. P2-2 운영점·`--cv`·`--label-window`. §2·§4·§6·§7 갱신.
- 2026-09-23: 윈도우 집계 컬럼명 결함 수정. `_mean`/`_std` 접미사를 **`feat_cols`에 든 컬럼에만** 붙이도록 조건을 걸었다 — 이전에는 `mean`으로 집계하는 `non_welding`까지 `non_welding_mean`이 되어 `--exclude-non-welding`이 `KeyError`로 죽고 점수 CSV에서 열이 빠졌다. 모델 입력·점수는 불변(재학습 불필요, 전 윈도우 점수 동일함을 실측 확인). `test_2b_window_columns`로 고정.
- 2026-09-24: 문서 역할 분리. `readme.md`·`test.md`는 최신 결과만(test.md를 현재 46피처 모델 기준으로 재작성, readme의 〔수정〕 경위·5.9 진행 이력·5.11 변경 요약 제거), 경과는 신규 `history.md`로 이동(구 test.md 전문 포함). 이 파일의 `test.md` N절 참조를 `history.md`(구 test.md N절)로 변경. §6에 규약 추가.
- 2026-09-24: **MVP 리뷰 반영**(`history.md` 9절). 종료 코드 규칙을 에피소드 시작 1회 + 30분 쿨다운으로(서빙·평가 동일, 평가는 4개 코드 전부 — 테스트 오트리거 0.25회/일/건 드러남), 지속 알람을 요청 횟수 → 시간 기준(180초), 요청 한도 1,200행(422) + 청크 내 모든 윈도우 스코어링, 비용접 carry를 버퍼 밖까지(실데이터 500 수정), `rule_code`/`rule_class_hint` 등 필드 추가, `.py/replay.py` 추가. 모델 재학습(동일 결과), 지표 갱신. §4·§5·§6·§7 갱신.
- 2026-09-25: **ML → RAG 핸드오프** 추가(`.py/rag_mapping.py`, `.py/mock_rag.py`, `main.py` 스키마·`/handoffs`·`handoff` 필드, `replay.py` 이벤트에 상황 ID·요약, `tests/test_rag_mapping.py`, `docs/*.example.json`(실데이터), readme 5.10 개발 스펙·테스트, history.md 10절). 범위는 ①②(증상 번역 + 상황 ID)로 한정. §5에 계약 추가. httpx를 본 의존성으로.
- 2026-09-26: **성능 점검 P3**(`history.md` 11절, `results/p3/`). 평가에 건별 AUROC(`auroc_gun_mean`)·규칙 이전 AUROC(`auroc_pre_rule`) 추가, 종료 코드 규칙의 24h 반복 발화 강등(`rule_repeat`, 번들 `rule.repeat_s`, 윈도우 `terminal_idx`; 평가·서빙·`replay.py` 동일), 레이블 창 1시간 유지 결정, 설정값 잔차·정비 이력 피처·발화 분류기 채택 안 함, 워밍업 윈도우 학습 혼입은 의도된 것으로 기록. `experiments.py` P3·`cache-maint`, docstring의 c19 포함 캐시 설명. `CLAUDE.md`의 명령·아키텍처를 실제 프로젝트에 맞게 수정. 모델 재학습(점수·임계값 동일). §2·§4·§5·§7 갱신.
- 2026-09-26: readme 5.7·5.8·5.9의 핸드오프 중복 서술을 5.10으로 일원화(포인터만 남김), 5.0 흐름도의 "미구현" 정정. `rag_mapping`이 반복 발화(`rule_repeat`)를 규칙 근거로 쓰던 결함 수정 + 단위 테스트(12개). 5.10 인용 문서(활용정리)가 저장소 밖임을 명시. §5 갱신.
- 2026-09-26: `techspec.md` 작성 — 남은 개발 항목(ML-1~7, HO-1~5)의 우선순위·설계·완료 기준, 모델 교체 게이트(4-fold `auroc_pre_rule` + 폴드 sd 이상 개선), 결정 필요 사항. §6에 문서 역할 추가.
