# techspec.md — 남은 개발 명세 (ML 파트 + RAG 핸드오프)

이 문서는 **앞으로 만들 것**만 담는다. 지금 구현된 상태는 `readme.md`(5.6 학습·평가, 5.7 API, 5.10 핸드오프 v1.0), 성능은 `test.md`, 결정 근거는 `MEMORY.md`, 경과는 `history.md`에 있다. 항목을 끝내면 여기서 지우고 결과를 그쪽 문서에 반영한다.

## 0. 범위

| 포함 | 제외 (다른 담당) |
|---|---|
| 이상탐지 모델·학습·평가(`.py/train.py`, `.py/experiments.py`) | 온톨로지(원인·점검·매뉴얼 조회) |
| 종료 코드 규칙과 서빙의 ML 로직(`main.py`의 스코어링·규칙·건별 상태) | RAG 검색, LLM 리포트, Slack 알림 |
| RAG로 넘기는 핸드오프 문서(`RagHandoff`, `.py/rag_mapping.py`)와 전달(outbox·push) | 배포·인증·인프라 일반 |

경계: ML은 **① 이상 감지 + ② 증상 번역(상황 ID)**까지 하고 `RagHandoff`를 넘긴다. RAG가 돌려주는 응답은 받아서 보관만 한다(5.10.6 계약).

## 1. 한눈에 보기

| ID | 항목 | 우선순위 | 선행 조건 | 크기 |
|---|---|---|---|---|
| ML-1 | 건별 서빙 상태 영속화 | **P0** | — | 중 |
| HO-1 | outbox 영속화 + push 재시도·멱등 | **P0** | — | 중 |
| HO-2 | 온톨로지 입력 형식 연동 | **P0** | 온톨로지 담당의 입력 형식·주소 | 소~중 |
| ML-2 | 워밍업 조건을 "정상 용접 N행"으로 | P1 | — | 소 |
| ML-3 | 서빙 윈도우를 분 경계로 정렬 | P1 | — | 소 |
| ML-4 | 건 프로파일로 다른 클래스 종료 코드 거르기 + 코드별 쿨다운 | P1 | 건별 고장 유형 정보(현장) | 중 |
| ML-5 | 운영 모니터링(건별 알람률·규칙 발화·드리프트) | P1 | ML-1 | 중 |
| HO-3 | 핸드오프 v1.1 — 맥락 확장 | P1 | HO-2(필드 이름 합의) | 중 |
| HO-4 | RAG 응답 검증 | P1 | RAG 응답 계약 확정 | 소 |
| ML-6 | 새 정보원 수용 경로 + 모델 교체 게이트 | P2 | 파형·정비 기록 확보 | 대 |
| ML-7 | 남은 데이터 분석 3건 | P2 | — | 소 |
| HO-5 | 증상 규칙 임계값 보정 + 예시 문서 갱신 | P2 | — | 소 |

P0 = 운영 투입 전에 반드시, P1 = 운영 품질, P2 = 성능·정확도 개선.

## 2. ML 파트

### ML-1 건별 서빙 상태 영속화 (P0)

- **현재**: `GunState`(워밍업 행·건 평균·건별 임계값, 알람 진행 상태, 규칙 쿨다운·반복 이력, 비용접 carry)가 메모리에만 있다. 서버 재시작 = 모든 건이 6시간 워밍업부터 다시, 쿨다운·24h 반복 판정 초기화.
- **목표**: 재시작해도 같은 판정을 이어간다.
- **설계**
  - 저장 대상(작다): `norm_status, norm{mean, std, t_end}, gun_threshold, t0, warmup_rows, rule_last_trigger, rule_last_by_code, rule_prev_code, rule_seen, alarm_since, consecutive_alarms, last_end, was_critical, carry`. 워밍업 중인 건은 `warm_parts`(최대 6시간 × 1 Hz)도 필요하다 — parquet로 따로 두거나, 워밍업 중 재시작은 워밍업 재시작으로 허용(결정 필요, §5).
  - 30분 버퍼(`rows`)는 저장하지 않는다. 재시작 후 첫 요청은 버퍼가 비어 있으니 60초 모일 때까지 202 — 지금과 같다.
  - 저장 시점: 상태가 바뀐 요청 끝에 건 단위로 원자적 쓰기(`RSW_STATE_DIR/<gun_id>.json`, 임시 파일 → rename). 기동 시 전부 로드.
  - 번들이 바뀌면(모델 `created`가 다르면) 건 평균은 유지해도 되지만 건별 임계값은 새 모델 점수 기준이라 무효 → 워밍업 재시작. 상태 파일에 `model_created`를 같이 저장해 비교한다.
  - `DELETE /guns/{id}`는 상태 파일도 지운다.
- **완료 기준**
  - pytest: 워밍업이 끝난 건을 스트리밍하다 앱을 재생성(TestClient 재기동)해도 `gun_norm="gun"`, `gun_threshold`, 쿨다운(30분 내 재발화 없음), `rule_repeat` 판정이 재시작 없이 돌린 결과와 같다.
  - 모델을 바꿔 재기동하면 그 건은 `warming_up`으로 돌아간다.

### ML-2 워밍업 조건을 "정상 용접 N행"으로 (P1)

- **현재**: 첫 6시간(`gun_norm.warmup_s`) 안의 정상·용접 행 평균. 워밍업 동안 대기 상태였던 건(검증 E04_4)은 이후 정상 알람률 4.0%로 남는다.
- **설계**: 조건을 "정상·용접 행 `min_rows`개 이상 모일 때까지(예: 10,800행 = 용접 3시간), 단 최대 `max_warmup_s`(예: 24h)"로 바꾼다. 오프라인 `train.gun_norm_stats()`/`window_file()`과 온라인 `Detector.gun_normalise()`를 **같이** 고친다(한쪽만 고치면 학습·서빙이 어긋난다 — `MEMORY.md` §4). 번들 `gun_norm`에 `min_rows`·`max_warmup_s` 의미를 기록하고 구 번들(시간 기준)도 계속 읽는다.
- **완료 기준**: 64건 4-fold에서 건별 정상 알람률의 최대값·표준편차가 줄고(E04_4 4.0% → 2% 이하 목표), 평균 정상 알람률과 AUROC는 폴드 편차(±0.04) 안. `tests/test_pipeline.py`의 온라인=오프라인 점수·임계값 일치 단언 유지.

### ML-3 서빙 윈도우를 분 경계로 정렬 (P1)

- **현재**: 학습은 분 경계 정렬 윈도우(`00:01:00~00:01:59`), 서빙은 들어온 마지막 60초(트레일링). 최대 59초 어긋난다(readme 5.6.1 함정 (a)).
- **설계**: `Detector.window_ends()`가 "완결된 분 경계 윈도우"만 스코어링하게 바꾼다. 응답의 최신 윈도우는 마지막 완결 분 — 최대 59초 늦어진다(결정 필요, §5). 부분 윈도우는 스코어링하지 않는다.
- **완료 기준**: 실데이터 1건을 `replay.py`로 재생한 윈도우 점수가 `train.score_frame()`의 같은 윈도우 점수와 윈도우 단위로 일치(|차이| ≤ 1e-4, 워밍업 경계 제외).

### ML-4 건 프로파일로 다른 클래스 종료 코드 거르기 (P1)

- **현재**: 반복 강등 후에도 테스트 critical 오트리거 0.18회/일/건(10회). 남은 오트리거는 **전부 그 건의 고장 유형과 다른 클래스의 종료 코드**다(주로 E029). 서빙은 건의 클래스를 모르므로 거를 수 없다. 또 쿨다운이 코드를 가리지 않아 test_3처럼 E016 직후의 E029가 눌린다(`MEMORY.md` §7-9).
- **설계**
  - 선택 입력 `GunProfile{gun_id, expected_fault_classes: list[E01..E04] | null}` — `PUT /guns/{id}/profile`. 없으면 지금과 같다(모든 코드 critical).
  - 프로파일이 있으면 목록 밖 클래스의 코드는 `rule_triggered=true, rule_out_of_profile=true`, severity `warning`(반복 강등과 같은 처리), 핸드오프 없음.
  - 코드별 쿨다운(`rule_last_trigger`를 코드별로)은 프로파일과 함께 넣을 때만 켠다 — 프로파일 없이 켜면 오트리거가 는다.
  - 평가: `evaluate()`에 "파일 클래스를 프로파일로 줬을 때"의 규칙 기준선을 추가(오프라인에서는 파일 클래스가 곧 프로파일).
- **완료 기준**: 테스트·검증에서 프로파일 기준 critical 오트리거 ≈ 0, 적중 8/8·14/14 유지. 프로파일 없는 요청은 기존 응답과 바이트 단위로 같다(회귀 테스트).
- **필요한 것**: 현장에서 건별로 어떤 고장 유형에 노출되는지(건 모델·공정) 정보. 없으면 이 항목은 보류.

### ML-5 운영 모니터링 (P1)

- **현재**: 건별 상태는 `GET /guns`의 스냅샷뿐이다. 모델이 설계대로(정상 알람률 ~1%, 건별 편차 작음) 돌고 있는지 운영 중에 볼 수 없다.
- **설계**: 건별 일 단위 카운터(스코어링 윈도우 수, 알람, 보류, 지속 알람 에피소드, 규칙 발화 / 반복 / critical)를 ML-1 상태에 함께 저장하고 `GET /guns/{id}/stats?days=7`, `GET /stats`(전체)로 낸다. 드리프트 신호: 워밍업 후 정상 알람률이 3일 연속 3% 초과, 또는 규칙 critical 발화가 하루 2회 이상 → 응답의 `model` 옆에 `drift_warning`(추가 필드) 또는 로그 경고.
- **완료 기준**: `replay.py`로 테스트 8건 전체 재생 후 `/stats`의 건별 알람률이 오프라인 `evaluate()`의 `per_file.alarm_rate_normal`과 ±0.2%p 안(정상 구간 정의 차이 감안).

### ML-6 새 정보원 수용 경로 + 모델 교체 게이트 (P2)

- **현재**: 데이터셋 안에서 만들 수 있는 피처(윈도우 통계, 이력·추세, 설정값 잔차, 정비 이력)는 모두 시도했고, 고장 10분 이전 분리력(`auroc_pre_rule`)은 ~0.6이 천장이다(`history.md` 8·11절). 모델을 바꿔서는 안 오른다.
- **필요 데이터 (현장 요청 목록)**
  1. 용접 1타 단위 요약: 타점마다 가압력·전류·통전 시간·전극 변위의 피크/적분(c11 증분 시각과 조인 가능한 키). 1 Hz로는 4~7초짜리 1타를 못 본다.
  2. 정비 기록: 캡 교체·드레싱·부품 교체의 시각과 종류(건 ID 기준).
  3. 고장 레이블의 정의: 이 데이터셋의 "고장" = 종료 코드 + 10분(적중 72건 전부 10.0분). 현장 레이블이 같은 정의인지.
- **수용 설계**: 새 입력은 `SensorReading`과 별도 스트림(`POST /welds`, `POST /maintenance`)으로 받아 건 상태에 붙이고, 윈도우 피처에 합친다. 오프라인은 `preprocess.py`에 조인 단계 추가.
- **모델 교체 게이트(모든 모델·피처 변경에 공통)**
  - 비교는 64건 **4-fold**로(단일 분할은 ±0.04 흔들림).
  - 채택 조건: `auroc_pre_rule` 4-fold 평균이 현재 대비 **폴드 표준편차 이상** 오르고, 테스트 8건에서도 떨어지지 않으며, 정상 알람률 ≤ 1.5%, 건별 알람률 최대 ≤ 5%. `auroc`(풀링)만 오른 변경은 채택하지 않는다(마지막 10분 = 규칙 몫일 수 있다).
  - 누설 점검: 새 피처의 건 안 경과 시간 Spearman |ρ| > 0.5면 기각(c19 사례). 룩백 피처는 6시간 상한.

### ML-7 남은 데이터 분석 (P2)

| 질문 | 왜 | 방법 | 결과가 바꾸는 것 |
|---|---|---|---|
| 고장 직전 1h가 대부분 비용접인 건(E01_0, 85%)이 72건 중 몇 건인가 | 그런 고장은 비용접 게이트에 걸려 모델 알람이 보류된다 | 파일별 고장 전 1h `non_welding` 비율 분포 | 게이트 예외(종료 코드 전후) 필요 여부 |
| E02 수집 시기(2019~20 vs 2021) 분포 차이 | 건 간 오프셋이 시기에서 오는지 | 시기별 정상 구간 센서 분포 비교(건별 centering 후) | 시기별 스케일러 필요 여부 |
| `c16 = 8.7`(두꺼운 판) 구간이 많은 새 건 | 학습 파일은 1.3% 미만, 테스트 2건은 85~92% | 두 건의 건별 centering 전후 점수 분포 | 판 두께별 기준 필요 여부 |

## 3. RAG 핸드오프

### HO-1 outbox 영속화 + push 재시도·멱등 (P0)

- **현재**: outbox는 메모리 deque(최근 1,000건), push는 1회 시도(`RSW_RAG_TIMEOUT_S` 10초)·재시도 없음. 재시작하면 미전달 핸드오프가 사라진다.
- **설계**
  - 저장: 핸드오프를 만들 때 `RSW_STATE_DIR/outbox/<event_id>.json`에 먼저 쓰고(`HandoffRecord` 그대로), 전달 상태가 바뀔 때마다 갱신. 기동 시 `pending`/`failed` 레코드를 다시 큐에 넣는다.
  - 재시도: 지수 백오프(10초 → 30초 → 2분 → 10분, 최대 5회), 그 뒤 `delivery="failed"` 확정. 4xx(계약 위반)는 재시도하지 않는다.
  - 멱등: 같은 `event_id`를 여러 번 보낼 수 있다 — RAG 쪽은 `event_id`로 중복을 버린다(계약에 명시). 요청 헤더 `Idempotency-Key: <event_id>`.
  - 레코드 필드 추가: `attempts`, `last_attempt_at`, `next_attempt_at`.
  - 보존: 전달 완료 30일, 이후 삭제(설정값).
- **완료 기준**: 가짜 RAG(`.py/mock_rag.py`)를 끈 채로 이벤트를 만들고 서버를 재시작한 뒤 가짜 RAG를 켜면, 모든 이벤트가 정확히 한 번 `GET :8001/received`에 나타난다(중복은 `event_id`로 제거된 것만). pytest로 고정.

### HO-2 온톨로지 입력 형식 연동 (P0)

- **현재**: 온톨로지·RAG 쪽 입력 형식과 주소가 미정(readme 5.10.8-1). `RagHandoff` v1.0을 그대로 POST한다.
- **할 일**: 온톨로지 담당과 (1) 받는 주소·메서드, (2) 필드 이름·필수 여부, (3) 상황 ID 체계(S01~S10) 고정 여부, (4) 응답 형식을 합의한다. 이름만 다르면 **어댑터**(`rag_mapping.to_ontology(handoff) -> dict`)를 두고 `RagHandoff` 자체는 바꾸지 않는다. 의미가 다르면 v2.0 협의.
- **완료 기준**: 합의 문서의 예시 입력이 `docs/`에 들어가고, 어댑터 출력이 그 예시와 키·타입이 같다는 단위 테스트, 실제 온톨로지 엔드포인트에 `docs/rag_handoff.example.json`이 200으로 들어간다.

### HO-3 핸드오프 v1.1 — 맥락 확장 (P1)

- **현재**: 핸드오프는 이벤트 윈도우 하나의 요약(센서 편차, 고장 유형, 증상, 상황 ID)만 담는다. readme 3절의 "이상 감지 시점 앞뒤의 센서 수치·로그를 모아 컨텍스트 구성"은 부분 구현이다.
- **추가 필드(전부 선택·추가만 — 1.x 호환 규칙)**

| 필드 | 내용 | 이유 |
|---|---|---|
| `error_timeline[]` | 최근 1h의 에러 코드 에피소드 `{code, start, end, duration_s, class_hint}` | 종료 코드 전후의 잔고장(E003 등)이 원인 분석 단서. 진짜 발화의 21%가 직전 1h에 다른 코드를 동반했다(오트리거는 7%) |
| `sensor_trend[]` | 기여 상위 센서의 최근 30분 1분 평균 시계열(z) | LLM 리포트가 "언제부터 어떻게" 변했는지 말할 수 있게 |
| `recent_events[]` | 같은 건의 최근 24h 핸드오프 `{event_id, detected_at, situation_ids[0]}` | 반복 고장·연쇄 판단 |
| `trigger.rule_repeat_suppressed` | 같은 요청에 강등된 반복 발화가 있었는지 | 지금은 반복 발화가 핸드오프에서 사라진다(규칙 근거에서 제외) |
| `detector.reliability` | `{auroc_pre_rule, rule_lead_min_median, rule_false_per_day}` — 번들 지표 | RAG가 신뢰도 문구를 수치에 맞게 쓰도록(현재 `caveats`는 고정 문구) |

- **설계**: `error_timeline`·`sensor_trend`는 30분 버퍼로 부족하다 → 건 상태에 1분 요약 링버퍼(60~120개)를 둔다(ML-1과 함께 저장). `schema_version` "1.1".
- **완료 기준**: `tests/test_rag_mapping.py::test_schema_keys_match_main` 갱신, v1.0 소비자(필드 무시)가 깨지지 않음을 예시 JSON으로 확인, 실데이터 재생 이벤트 9건의 `error_timeline`에 종료 코드 에피소드가 들어 있다.

### HO-4 RAG 응답 검증 (P1)

- **현재**: push 응답이 JSON이면 `rag_response`에 그대로 저장한다. 형식 검증이 없다.
- **설계**: 5.10.6 계약을 Pydantic `RagResponse{event_id, status, generator, report{7 keys}}`로 정의. 검증 실패는 `delivery="delivered"` + `delivery_detail="invalid response: ..."`(전달은 됐고 응답만 틀림), `event_id` 불일치도 실패로 기록. 스키마는 `/openapi.json`에 노출해 RAG 쪽이 맞춰 볼 수 있게 한다.
- **완료 기준**: 가짜 RAG 응답은 통과, 키가 빠진 응답·다른 `event_id`는 `delivery_detail`에 사유가 남는 단위 테스트.

### HO-5 증상 규칙 임계값 보정 + 예시 갱신 (P2)

- **현재**: `MIN_SHARE` 0.05·`MIN_DEV` 1.0은 임의값, 증상 규칙(P1~P9)은 매뉴얼 기반이라 실데이터 이벤트 9건 중 1건만 맞았다. `docs/anomaly_result.example.json`은 `rule_repeat` 이전 형식이다.
- **설계**: 학습·테스트 80건 전체 재생의 critical 이벤트에서 `sensor_findings` 수 분포를 보고, 이벤트당 0~3개가 되도록 두 값을 정한다(정답 레이블이 없으므로 "과다·과소 보고 방지"가 목표). 증상 신뢰도는 계속 low/medium 상한 — 성능 근거가 생기기(ML-6) 전에는 올리지 않는다. 예시 3종을 현재 서버로 다시 뽑는다.
- **완료 기준**: 재생 이벤트의 findings 개수 중앙값 1~3, 예시 JSON이 현재 스키마로 검증된다.

## 4. 공통 규칙

- 학습·서빙이 같은 절차를 쓰는 로직(건별 정규화, 규칙, 윈도우)은 **오프라인 함수와 온라인 함수를 한 쌍으로** 고치고, `tests/test_pipeline.py`에 둘의 일치 단언을 둔다.
- `AnomalyResult`·`RagHandoff`는 계약이다: 필드 추가만, 삭제·의미 변경은 버전 올림과 하류 합의.
- 실험·모델 변경은 §ML-6 게이트로 판정하고 `history.md`에 절을 추가, `readme.md`·`test.md`는 결과만 덮어쓴다.

## 5. 결정이 필요한 것

| 질문 | 선택지 | 권장 |
|---|---|---|
| 워밍업 중 재시작 시 모은 행을 보존할까 (ML-1) | 보존(행 저장) / 워밍업 재시작 | 재시작 허용 — 저장량 대비 이득 작음 |
| 분 경계 정렬로 최신 윈도우가 최대 59초 늦어져도 되나 (ML-3) | 정렬 / 트레일링 유지 | 정렬 — 규칙이 이미 ~10분 선행을 맡으므로 1분 지연은 영향 작음 |
| 건별 고장 유형 정보를 받을 수 있나 (ML-4) | 받는다 / 없음 | 받을 수 있으면 P1, 없으면 보류 |
| 온톨로지가 받을 형식 (HO-2) | v1.0 그대로 / 어댑터 / v2.0 | 어댑터 |
| 현장 데이터(파형·정비 기록) 확보 여부 (ML-6) | — | 확보 전에는 모델 성능 작업을 하지 않는다 |
