# 실험·변경 기록 (history)

무엇을 언제 왜 바꿨고, 그때 어떤 수치가 나왔는지를 **시간순으로** 남기는 곳이다. 지금 상태(최신 설계·결과)는 `readme.md`와 `test.md`, 결정의 근거 요약은 `MEMORY.md`에 있다. 새 실험이나 변경을 하면 여기에 절을 추가하고, readme·test.md는 결과만 최신으로 고친다.

## 연표

| 날짜 | 내용 |
|---|---|
| 09-17 ~ 09-18 | 논문 리뷰, 데이터 확보, 원본 CSV 점검(`file_check.py`, `error_check.py`), 접근 방식 결정(1-step 이상탐지, readme 2절) |
| 09-22 | 벤치마크 코드 리뷰·수정(5.2), EDA(5.3), 전처리(5.4), IsolationForest 베이스라인(5.6), FastAPI 서빙(5.7), `MEMORY.md` 작성 |
| 09-23 | 테스트 8파일 전처리·평가 추가(5.5, 5.6). 파이프라인 점검: gap 채움 버그(ffill+bfill로 120초까지 메꿔짐) 수정 후 전처리·재학습, 번들에 전처리 설정 포함·서빙이 읽도록 변경, E2E 스모크 테스트·`requirements.txt`·`pyproject.toml` 추가 |
| 09-23 | 문서 정리(0~4절 사실 정정, 5.3/5.4.1/5.6.1 상세화). 그 과정에서 윈도우 컬럼명 결함을 발견해 수정하고 회귀 테스트 추가 |
| 09-23 | 테스트 판정(불충분) 후 P0-1 실행: c16 `>5` 분기가 두꺼운 판 용접을 비용접으로 오분류한 것을 확인해 규칙을 `c16 <= 0`으로 변경, 전처리 재실행, 학습 기본 `--exclude-non-welding`, 평가·서빙에 비용접 알람 게이트 추가. 테스트 정상 알람률 3.2%→0.7%, test_2·test_3 지속 오탐 0/일 (아래 구 test.md 6절) |
| 09-23 | P0-2 실행: 워밍업(첫 6시간) 기반 건별 centering + 건별 임계값을 학습·평가·서빙에 같은 절차로 구현(`--gun-norm`, 번들 `gun_norm`, API `gun_norm`/`gun_threshold`). center/scale × 임계값 절제 후 center+임계값 채택, scale 폐기. 테스트 건별 정상 알람률 0~1.7% → 0.03~0.9% (아래 구 test.md 7절) |
| 09-23 | P1·P2 전부 실행(`.py/experiments.py`, `results/p1p2/`): 이력·추세 피처, 5분 창, EWMA/CUSUM, LSTM-AE, 지도학습 상한선, E02 분리, 레이블 창 민감도, 건 단위 4-fold. **c19가 시간 카운터(레이블 누설)임을 발견**해 입력에서 제거(46피처)하고 재학습. 정직한 상한선 = 비지도 수준 → 피처(정보) 문제로 확정, 모델 변경 채택 없음. 종료 코드 규칙 → API `rule_triggered`, 평가에 운영점·규칙 기준선·`--cv`·`--label-window` (아래 구 test.md 8절) |
| 09-24 | 문서 역할 분리: `readme.md`·`test.md`는 최신 결과만, 수정 기록·실험 경과는 `history.md`(이 파일)로 이동 |

## 지표 추이 (같은 테스트 8건 / 검증 14건)

| 단계 | 모델 입력 | 테스트 AUROC | recall@1h | 정상 알람률 | 건별 정상 알람률 | 검증 AUROC | 고장으로 이어지는 지속 알람 (테스트) |
|---|---|---|---|---|---|---|---|
| 09-22 베이스라인 (구 c16 규칙) | 48 | 0.687 | 5.4% | 3.2% | 0~13.2% | 0.630 | 1/8 (인공물) |
| P0-1 비용접 규칙·게이트 | 48 | 0.679 | 4.6% | 0.7% | 0~1.7% | 0.642 | 0/8 |
| P0-2 건별 정규화·임계값 | 48 | 0.639 | 2.9% | 0.5% | 0.03~0.9% | 0.684 | 0/8 |
| **c19 제거 (현재 `models/`)** | **46** | **0.636** | **2.9%** | **0.5%** | **0.02~1.0%** | **0.647** (4-fold 0.631 ± 0.038) | **0/8** |
| (참고) 종료 코드 규칙만 | — | — | — | — | — | — | 8/8, 선행 중앙값 9.1분 |

읽는 법: 오탐(정상 알람률, 건별 편차)은 설계 목표대로 잡혔고, 탐지(AUROC, recall@1h)는 P0 전보다 낮아 보이지만 그 차이는 인공물(비용접 오분류, 건 오프셋, c19 추세)이 빠진 것이다. 어떤 단계에서도 운영점(선행 30분·오탐 1회/일)을 만족한 테스트 건은 없다.

## 2026-09-17 ~ 09-22 — 초기 구상에서 바뀐 결정

readme 초판의 구상이 EDA(72파일) 후 어떻게 바뀌었는지.

| 초기 구상 | 바뀐 결정 | 이유 |
|---|---|---|
| 진단 규칙으로 도출된 최종 에러(E01~E04)를 정답으로 삼아 센서 패턴에서 역산하는 **분류** | 정상 구간만으로 학습하는 **비지도 이상탐지**, 클래스는 종료 코드 매핑(`known_code_class_hint`)으로 보조 | 클래스당 18건뿐이라 양성 표본 부족, 정상 구간은 파일마다 6일 이상 |
| `error` 컬럼에 잔고장이 평소보다 자주 뜨는 것 자체가 전조 → "최근 10분 누적 횟수"를 입력 피처로 | `error_share_10min`(최근 10분 중 에러 상태 비율) | `error`는 이벤트가 아니라 수 시간 지속되는 상태(E003 중앙 1.5~2h, E011 최장 27.9h). 횟수로 세면 28시간짜리 상태가 1회. E003·E011·E029는 며칠 전부터 떠 있어 전조로는 약하다 — 가설은 절반만 맞음 |
| 20개가 넘는 에러 코드 중 파일 끝에 몰리는 코드만 Target=1로 이진화 | 코드는 11종. 레이블은 **시간 기준**(`ttf_s ≤ 3600` = 1), 종료 코드는 `terminal_code` 플래그와 클래스 힌트로만 | 종료 코드는 고장 10분 전에야 뜬다 — 정답으로 쓰면 "10분 전 탐지"밖에 안 된다 |
| 결측·이상치를 보간할지 남길지 미결 | 규칙 처리: ≤60초 gap ffill, 긴 gap은 세그먼트 분리, 비용접은 플래그 | gap 99%가 ≤35초, 최장 1시간 남짓 |
| 비용접 행을 60초 한도로 carry | 길이 제한 없이 carry | 60초 제한이면 고장 직전 1시간이 날아가는 파일(E01_0)이 있었음 |
| 작업 비중 예상: 전처리 50%, 학습·튜닝 30%, 하류 20% | 실제로도 전처리가 가장 오래 걸렸다 | |

문서 정정(09-23): error 코드 20종 → 실제 11종, 원본 4,243만 행, 결측 40% 초과 E02 8개, 파일명 `test_0.csv`/`E01_0.csv`, c9 = Start friction, c16 임계는 논문 본문 "6 초과"가 아니라 벤치마크 코드 5.

## 2026-09-23 — 파이프라인 점검에서 고친 결함

- **gap 채움**: `ffill(60).bfill(60)`이라 실제로는 120초까지 메꿨다(학습 15개 파일·test_5에 해당 gap). 합성 데이터 스모크 테스트가 잡았다. `preprocess.fill_gaps()` 하나로 통일하고 `main.py`도 이것을 import한다. 전처리·재학습.
- **번들 ↔ 서빙**: 서빙이 전처리 파라미터를 하드코딩하던 것을 번들의 `preprocess_config`·`scaler`에서 읽도록 변경. per-file 스케일·resample 번들은 기동 거부.
- **윈도우 컬럼명**: `_mean`/`_std` 접미사를 모든 집계에 붙여 `non_welding`이 `non_welding_mean`이 됐고, `--exclude-non-welding`이 `KeyError`로 죽고 점수 CSV에서 `non_welding` 열이 조용히 빠졌다. 접미사를 `feat_cols`에만 붙이도록 수정. 모델 입력·점수는 불변(전 윈도우 점수 동일 실측), `test_2b_window_columns`로 고정.
- **c16 규칙 변경**(`c16 > 5 or c16 ≤ 0` → `c16 ≤ 0`): 아래 P0-1(구 test.md 6절). 규칙 변경 전 비용접 비율은 학습 파일당 0.2~44.6%·평균 11.3%(EDA 표 기준 E01 11.1 / E02 15.1 / E03 9.9 / E04 9.2%), 테스트 test_2·test_3은 95%·87%였다.
- 서빙 검증 이력: P0-1 시점 test_3 게이트 보류 윈도우 점수 0.50472 일치, P0-2 시점 test_0 첫 6.5시간 25분 청크 → 점수 0.45415 일치, 건별 임계값 0.5770 vs 0.5772.

### 2026-09-23 변경 요약 (영역별)

| 영역 | 변경 전 | 변경 후 | 왜 / 효과 | 상세 |
|---|---|---|---|---|
| 테스트셋 처리 | 학습 72파일만 전처리 | `preprocess.py --split test`: 8파일을 학습 설정·스케일러로 전처리, 마지막 10분 종료 코드로 클래스 추론(`class_source`), `train.py`가 학습 직후 테스트 자동 평가 + `--evaluate` | 테스트 지표를 학습과 같은 함수·임계값으로 | 5.5, 5.6 |
| gap 채움 | `ffill(60).bfill(60)` → 실제로는 120초까지 메꿈 | `fill_gaps()` 하나(≤60초 ffill, 초과는 행 삭제·세그먼트 분리), 서빙도 같은 함수를 import | 합성 데이터 스모크 테스트가 잡은 버그. 학습 15파일·test_5에 해당 gap 존재 | 5.4 |
| 번들 ↔ 서빙 | 서빙이 전처리 파라미터를 하드코딩 | 번들에 `preprocess_config`·`scaler`, 서빙이 읽음. per-file 스케일·resample 번들은 기동 거부 | 학습·서빙 불일치 방지 | 5.6, 5.7 |
| 품질 관리 | 없음 | `tests/test_pipeline.py`(합성 E2E, 5개), `requirements.txt`, `pyproject.toml`(ruff/black) | 파이프라인 변경 시 30초 안에 회귀 확인 | 5.8 |
| 윈도우 컬럼명 | `non_welding`이 `non_welding_mean`으로 바뀜 | `_mean`/`_std` 접미사는 모델 피처에만 | `--exclude-non-welding`이 KeyError로 죽던 결함. 점수 불변 | 5.6.1 |
| 문서 | 0~4절에 사실 오류(error 코드 20종→실제 11종, 원본 행 수 등) | 0~4절 정정, 5.3.1~5.3.7 EDA 상세, 5.4.1 파생 피처, 5.6.1 윈도우 상세 | | 0~5절 |
| 테스트 판정 | 없음 | `test.md`: 판정 불충분, 원인 6가지, P0~P2 개선 목록 | 이후 작업의 근거 | `test.md` 1~5 |
| **P0-1 비용접 규칙** | `c16 > 5 or c16 <= 0` = 비용접 | **`c16 <= 0`만**(`--outlier-hi` 기본 None). 학습 기본 `--exclude-non-welding`, 비용접 비율 > 0.5 윈도우는 알람 보류(`--alarm-max-non-welding`, API `alarm_held`) | `c16 > 5`는 두꺼운 판 setpoint(학습에서 시간당 800~980회 용접). test_2·test_3의 87~95%를 비용접으로 오분류해 오탐의 전부를 만들었음. 테스트 정상 알람률 3.2% → 0.7% | 5.4, 5.5, `test.md` 6 |
| **P0-2 건별 정규화** | 글로벌 z-score + 글로벌 임계값(건별 정상 알람률 0~13%) | 워밍업(첫 6시간 정상·용접 행) 평균을 빼는 `--gun-norm center` + 건별 임계값 max(글로벌, 워밍업 99% 분위수). 서빙이 온라인으로 같은 절차(`gun_norm`, `gun_threshold`) | 건별 정상 알람률 테스트 0.03~0.9%. std까지 나누는 `scale`은 대기 건에서 56.5%로 폭주해 폐기 | 5.6, 5.7, `test.md` 7 |
| **P1 실험** | 미실행 | `.py/experiments.py`로 이력·추세 피처, 5분 창, EWMA/CUSUM, LSTM-AE, 지도학습 상한선, E02 분리 실행 → **채택 없음** | 정직한 상한선(c19 제외) 0.62~0.70 = 비지도 수준 → 피처(정보) 문제 | `test.md` 8 |
| **c19 누설** | c19를 센서로 입력 | `--drop-features c19` 기본, 46피처로 재학습 | c19는 초당 ~55씩 증가하는 카운터 = 7일 파일에서 "고장까지 남은 시간". 지도학습 AUROC 0.98이 전부 이것 | 5.6, `test.md` 8.1 |
| **P2-1 종료 코드 규칙** | `known_code_class_hint`만 제공 | 규칙이 뜨면 `severity=critical` 강제(`rule_triggered`, `severity_source`), 평가에 규칙 기준선 | 모델이 종료 상태를 임계값 아래로 보는 문제를 규칙으로 메움. 보장 선행 ~10분 | 5.7 |
| **P2-2 평가 설계** | AUROC·알람률·파일별 선행 | + 운영점(선행 ≥30분 & 오탐 ≤1/일/건 충족 건 수), `--cv k` 건 단위 k-fold, `--label-window`, 종료 코드 규칙 기준선 | 목표를 먼저 명시하고 분할 불확실성(±0.04)을 같이 봄 | 5.6 |

### 2026-09-23 바뀐 파일

| 파일 | 변경 |
|---|---|
| `.py/preprocess.py` | `--split test`, 클래스 추론, `fill_gaps()`, c16 규칙 `<= 0`(`--outlier-hi`), 설정·스케일러 상속 |
| `.py/train.py` | 테스트 평가·`--evaluate`, 비용접 게이트, 건별 정규화·임계값(`window_file`, `gun_thresholds`), `--drop-features`(c19), `--cv`, `--label-window`, 운영점·규칙 기준선, 윈도우 컬럼명 수정 |
| `.py/experiments.py` (신규) | P1·P2 실험 실행기: 윈도우 캐시 → 실험 → `results/p1p2/*.json`, `summary` |
| `main.py` | 번들의 전처리 설정·게이트·건별 정규화 재현, `alarm_held`/`hold_reason`, `gun_norm`/`gun_threshold`, `rule_triggered`/`severity_source`, `/model`에 `gun_norm`·`dropped_features` |
| `tests/test_pipeline.py` (신규) | 합성 데이터 E2E 5개: 전처리 → 테스트 split → 윈도우 컬럼 → 학습·평가·스코어 → API 일치(워밍업 전·후, 게이트, 규칙) |
| `test.md` (신규) | 판정과 원인(1~5절), P0-1(6), P0-2(7), P1·P2(8) 실행 결과 |
| `MEMORY.md` | §2~§7 갱신, 변경 이력 9줄 |
| `results/p0/`, `results/p1p2/` (신규) | 절제 실험 지표 JSON(모델 파일·캐시는 제외) |
| `models/` | 46피처 모델·지표·점수 CSV 재생성 |
| `requirements.txt`, `pyproject.toml`, `.gitignore` | 신규 / 캐시·데이터 폴더 제외 |

## 2026-09-23 — 테스트 판정과 P0·P1·P2 실험 (구 `test.md` 전문)

아래는 09-23에 작성·갱신한 `test.md`를 그대로 옮긴 것이다. **본문의 "N절" 참조는 구 test.md의 절 번호**(1~5 = P0 전 진단, 6 = P0-1, 7 = P0-2, 8 = P1·P2)다. 수치는 각 시점의 모델 기준이고, 경로(`models/`)는 이후 실행으로 덮어써졌다 — 단계별 지표 JSON은 `results/p0/`, `results/p1p2/`에 있다.

대상: `models/baseline_iforest.joblib` (IsolationForest, 60초 윈도우, 임계값 0.5411 = 학습 정상 점수 99% 분위수, sustain 3)
**2026-09-23 갱신**: 1~5절은 P0 실행 **전** 모델의 진단이다. P0-1(비용접) 결과는 **6절**, P0-2(건별 정규화·임계값) 결과는 **7절**, P1·P2 전체 결과는 **8절**에 있다(같은 파일 경로에 덮어씀). 현재 `models/`는 8.5절의 모델(c19 제거, 46피처)이다. **8.1의 발견(c19 = 시간 카운터 = 레이블 누설)이 이 문서에서 가장 중요하다.**
데이터: `test/` 8파일 → `preprocessed/test/` (학습과 같은 전처리·스케일러) → 80,533 윈도우
재현: `python .py/train.py --evaluate` → `models/baseline_iforest_test_metrics.json`, `models/scores/test_N_iforest.csv`

### 1. 판정

**충분하지 않다.** 지금 모델은 규칙 하나(종료 코드 → 클래스 힌트)보다 못하다.

| 기준 | 목표 (3절 알람 피로도·RAG 트리거 요구) | 현재 | 판정 |
|---|---|---|---|
| 고장으로 이어지는 지속 알람(3회 연속)이 뜨는 건 수 | 8건 중 과반 | **1건** (test_3) | ✗ |
| 그 알람의 선행 시간 | 30분 이상 | **1.6분** | ✗ |
| 고장 전 1시간 윈도우 재현율 | 참고 지표 | 5.4% | ✗ |
| 정상 구간 지속 오탐 | 건당 하루 1회 이하 | 6건은 0~0.17회, **2건은 7.2·8.3회** | ✗ |
| 모델 없이 되는 기준선(종료 코드 규칙) | 이겨야 함 | 규칙은 8/8건에서 7.5~10분 전에 클래스까지 맞힘 | **규칙이 이김** |

즉 "고장 1시간 전 탐지"는 거의 못 하고, "고장 10분 전 확인"은 이미 규칙이 더 잘 한다. 모델이 파이프라인에 보태는 것이 현재는 없다.

### 2. 결과

#### 2.1 전체·클래스별 (고장 전 1h vs 정상 윈도우)

| | AUROC | AUPRC | recall@1h | 정상 알람률 | 에러 상태 알람률 |
|---|---|---|---|---|---|
| 전체 (8건) | 0.687 | 0.014 | 0.054 | 0.032 | 0.011 |
| E01 (2건) | **0.452** | | 0.000 | 0.002 | |
| E02 (1건) | 0.800 | | 0.167 | **0.110** | |
| E03 (2건) | 0.887 | | 0.017 | 0.000 | |
| E04 (3건) | 0.678 | | 0.077 | 0.044 | |

검증(14건)과 같은 모양이다: 전체 AUROC 0.630, recall@1h 0.044, 정상 알람률 0.014. 테스트 정상 알람률이 두 배인 것은 아래 2.2의 두 파일 때문이다.

#### 2.2 파일별

| 파일 | 클래스 | 마지막 지속 알람 시작 (고장 전) | 지속 오탐 / 일 | 정상 알람률 | 고장 전 1h 알람률 | 비용접 비율 (전체 / 고장 전 1h) |
|---|---|---|---|---|---|---|
| test_0 | E01 | 없음 | 0.00 | 0.3% | 0% | 12% / 0% |
| test_1 | E01 | 없음 | 0.00 | 0.0% | 0% | 3% / 3% |
| test_2 | E02 | 없음 | **8.3** | **11.0%** | 17% | **95% / 72%** |
| test_3 | E04 | **1.6분** | **7.2** | **13.2%** | 23% | **87% / 72%** |
| test_4 | E03 | 없음 | 0.00 | 0.0% | 2% | 0% / 0% |
| test_5 | E03 | 없음 | 0.00 | 0.0% | 2% | 5% / 22% |
| test_6 | E04 | 없음 | 0.17 | 0.1% | 0% | 12% / 2% |
| test_7 | E04 | 없음 | 0.17 | 0.0% | 0% | 10% / 1% |

#### 2.3 점수가 고장에 가까워지며 움직이는가 (중앙값 / 알람률)

| 클래스 | 24h 초과 | 6~24h | 1~6h | 10분~1h | 마지막 10분 |
|---|---|---|---|---|---|
| E01 | 0.426 / 0.2% | 0.428 / 0.1% | 0.425 / 0% | 0.419 / 0% | 0.425 / 0% |
| E02 | 0.403 / 12% | 0.397 / 1.7% | 0.466 / 17% | 0.456 / 10% | 0.536 / 50% |
| E03 | 0.377 / 0% | 0.397 / 0% | 0.396 / 0% | 0.450 / 1% | 0.472 / 5% |
| E04 | 0.431 / 4.6% | 0.431 / 2.7% | 0.429 / 5.3% | 0.443 / 4% | 0.467 / 26% |

E01은 7일 내내 점수가 평평하다. 나머지도 **마지막 10분**에서야 오르고, 그마저 알람률은 5~50%다.

### 3. 왜 부족한가

#### 3.1 이 피처로는 1시간 전에 갈라지는 신호가 거의 없다

테스트 윈도우에서 고장 전 1h와 정상의 효과 크기(Cohen's d)가 0.5를 넘는 피처는 **48개 중 9개**뿐이고, 전부 **`_std`(변동성)와 용접 활동량**이다. 논문의 타깃인 `c5`(밸런스 압력)·`c2`(전극 압력)의 **평균은 거의 움직이지 않는다**(E01: c5 0.16 vs 0.10, z-score 단위). 평균 중심의 48피처 공간에서 IsolationForest가 잡을 것이 없다.

| 상위 피처 | d | 고장 전 1h | 정상 |
|---|---|---|---|
| c13_std | 0.57 | 0.83 | 0.47 |
| c18_std | 0.57 | 0.85 | 0.51 |
| c4_std | 0.54 | 0.86 | 0.52 |
| c3_std | 0.53 | 0.44 | 0.27 |
| c2_std | 0.52 | 0.73 | 0.44 |
| c5_std | 0.46 | 0.52 | 0.33 |

→ 전조가 있다면 **값이 아니라 흔들림**에 있다. 60초 std 하나로는 약하고, 더 긴 시간 축의 변동성·추세 피처가 필요하다.

#### 3.2 모델이 종료 상태조차 못 본다

에러 상태(`error_active=1`) 윈도우의 알람률이 **1.1%**다. E029 같은 종료 코드가 떠 있는 마지막 수 분도 임계값 아래로 본다. 이유는 `error_code`가 입력이 아니고(`error_share_10min`만 입력) 10분 롤링 비율은 종료 코드가 뜬 뒤 수 분이 지나야 커지기 때문이다. 이 구간은 어차피 규칙(`known_code_class_hint`)이 맡으므로 치명적이진 않지만, 모델이 "명백한 이상"에도 반응하지 않는다는 뜻이다.

#### 3.3 비용접 구간이 오탐과 "성능"을 동시에 만든다

오탐의 거의 전부가 test_2·test_3에서 나온다. 두 파일은 행의 87~95%가 `non_welding`이다(학습 파일 최대 45%). 이 구간은 센서값을 직전 용접값으로 **이어 붙인 상수**라 두 가지 왜곡이 생긴다.

- **test_2**: 정상 구간의 *용접* 윈도우 알람률 **66.7%**, *비용접* 윈도우 11%. 이 건은 "거의 안 용접하는 상태"가 기준이 되어 **정작 용접할 때가 이상으로 잡힌다.**
- **test_3**: 비용접 윈도우 13.2%, 용접 0%. 이어 붙인 setpoint(c18·c13·c4)가 학습 분포와 달라 상시 경고.
- 두 파일 모두 **고장 전 1h의 72%가 비용접**이다. E02 AUROC 0.80, E04 0.68은 상당 부분 이 인공물이다.

세 번째 가능성: 이 두 건에서는 c16 규칙(>5 또는 ≤0 = 캡 드레싱)이 틀렸을 수 있다. 87~95%를 캡 드레싱으로 보내는 것은 물리적으로 이상하다. 확인 전에는 E02·E04 수치를 믿으면 안 된다.

#### 3.4 건 간 분포 차이를 글로벌 임계값 하나로 자른다

정상 구간 점수 중앙값이 건마다 0.375(test_4)~0.471(test_3)로 다르다. `c7`·`c8`·`c9`는 건 고유 상수라 z-score 후 사실상 건 ID가 되고, 글로벌 스케일러는 학습에 없던 건의 오프셋을 그대로 점수에 싣는다. 그 위에 임계값 0.5411 하나를 쓰니 건별 정상 알람률이 0%에서 13%까지 벌어진다. 오탐률 1% 설계는 **학습 건 평균**에서만 성립한다.

#### 3.5 모델에 시간 기억이 없다

IsolationForest는 윈도우 하나를 독립 표본으로 본다. 시간 축은 60초 std와 10분 롤링 3개, 그리고 사후의 sustain=3(=3분)뿐이다. 3.1의 결과대로 신호가 "서서히 커지는 변동성"이라면 독립 윈도우 점수로는 추세를 못 잡는다.

#### 3.6 평가 자체의 불확실성

- 테스트 8건, 검증 14건. 클래스당 1~3건이라 클래스별 수치는 건 하나에 좌우된다(E02는 test_2 하나).
- "마지막 3600초 = 고장 전조"는 가정이다. 3.1처럼 1시간 전에 갈라지는 신호가 약하면 어떤 모델도 recall@1h이 낮다. 레이블 창(30분·2h·6h)을 바꿔 민감도를 봐야 한다.

### 4. 개선해야 할 부분

우선순위는 "결과가 바뀔 확률 × 비용"이다. P0는 모델을 바꾸기 전에 반드시 해야 한다. 지금 상태로 모델을 바꾸면 3.3·3.4의 인공물을 같이 최적화하게 된다.

| 순위 | 항목 | 근거 | 방법 | 확인 지표 |
|---|---|---|---|---|
| **P0 ✓ 완료(6절)** | 비용접 구간 처리 | 3.3 | ① `--exclude-non-welding`으로 학습에서 제외 ② **스코어링에서도** 비용접 비율이 높은 윈도우는 알람 보류(API `non_welding_share` 기준) ③ test_2·test_3의 c16 분포를 보고 규칙이 맞는지 확인 | test_2·test_3 정상 알람률 → 1% 미만, E02·E04 AUROC 재측정 → **0.7%·0.3%, E02 0.86·E04 0.69 (달성)** |
| **P0 ✓ 완료(7절)** | 건별 정규화·임계값 | 3.4 | `--drop-static`(c7~c9 제거) + 건별 스케일. 서빙은 per-file 스케일러를 재현할 수 없으므로 **워밍업 구간(예: 첫 6시간)으로 건별 mean/std와 건별 99% 임계값을 온라인 추정**하는 방식으로 설계 | 건별 정상 알람률의 편차(현재 0~13%)가 1% 안팎으로 수렴 → **테스트 0.03~0.9%(sd 0.3%), 검증 0.1~4.9%(sd 1.2%). 평균만 빼는 center 채택, std까지 나누는 scale은 폐기** |
| **P1 ✓ 실행(8절)** | 변동성·추세 피처 | 3.1 | 5·10·30분 롤링 std, std의 기울기, 최근 1h 대비 24h 비율. `--window 300`도 같이 | recall@1h, 그리고 "마지막 지속 알람 시작 시각"이 10분 밖으로 나오는 건 수 → **10분 밖 0건, AUROC 차이는 폴드 편차 안. 채택 안 함** |
| **P1 ✓ 실행(8절)** | 시간 축을 가진 모델 | 3.5 | LSTM/1D-CNN 오토인코더(정상 구간 학습), 또는 점수 EWMA·CUSUM으로 추세 누적. 5.10절 실험 후보와 동일 | 위와 동일 + 정상 오탐/일 → **EWMA +0.01, CUSUM 발산, LSTM-AE 0.59. 채택 안 함** |
| **P1 ✓ 실행(8절)** | 지도학습 상한선 | 3.6 | `label`로 GBDT를 학습해 **이 피처로 도달 가능한 최대 AUROC**를 재본다. 비지도 결과가 그 근처면 피처 문제, 멀면 모델 문제 | AUROC 갭 → **c19 누설 제거 후 상한선 0.62~0.70 = 비지도와 동일. 피처(정보) 문제로 확정** |
| **P1 ✓ 실행(8절)** | 클래스별 분리 | 2.1·EDA | E02는 결측·수집 시기·에피소드 수가 전부 다르다(readme 5.3.1). E02 전용 모델 또는 E02 제외 평가 | 클래스별 지표 → **E02 전용 모델은 더 나쁨, E02 제외해도 전체 동일. 채택 안 함** |
| **P2 ✓ 완료(8절)** | 종료 상태 반응 | 3.2 | `error_active`를 입력에 넣지 말고, 대신 **규칙(`known_code_class_hint`)이 뜨면 severity를 강제로 critical로** 올린다. 모델이 못 보는 구간을 규칙으로 메운다 | 에러 상태 알람률 → 100% (규칙 기준) → **종료 상태 윈도우 100%, API `rule_triggered`/`severity_source`, 평가에 규칙 기준선 포함** |
| **P2 ✓ 완료(8절)** | 평가 설계 | 3.6 | 목표 운영점을 먼저 명시(선행 30분, 오탐 1회/일/건). 건 단위 k-fold, 레이블 창 민감도, 종료 코드 규칙을 **기준선**으로 표에 같이 둔다 | 표 형식 자체 → **`evaluate()`의 `operating_point`·`rule_terminal_code`, `--cv k`, `--label-window`** |

**바로 실행 가능한 순서**

1. ~~`python .py/train.py --exclude-non-welding` 후 `--evaluate` → 3.3이 오탐의 원인인지 확정~~ 완료(6.2 A행: 원인은 맞지만 게이트만으로는 두 건을 가릴 뿐)
2. ~~test_2·test_3의 `c16` 값 분포 확인 → 규칙 수정 여부 결정~~ 완료(6.1: `> 5` 분기 삭제)
3. ~~`--drop-static` + per-file 스케일 → 3.4 확인~~ 완료(7절: 워밍업 기반 건별 centering + 건별 임계값, 서빙까지 구현)
4. ~~`label` 지도학습으로 상한선 측정~~ 완료(8.1·8.3: c19 누설 발견, 정직한 상한선 = 비지도 수준 → 피처를 판다)
5. ~~그 다음에 P1 피처·모델~~ 전부 실행, 채택 없음(8.3). 다음은 새 정보원(8.4)

### 5. 참고

- 검증 14건 중 고장으로 이어지는 지속 알람이 뜬 건도 2건(E02_1 9분 전, E02_9 10분 전)뿐이었다. 테스트 결과는 검증에서 이미 보였던 것이고, 새로운 실패가 아니다.
- 종료 코드 규칙의 선행 시간: test_3 7.5분, 나머지 7건 10.0분(전처리의 `infer_class`가 마지막 10분만 보기 때문에 10분이 상한이다. 규칙 자체는 더 일찍 볼 수 있다).
- 수치 출처: `models/baseline_iforest_test_metrics.json`, `models/scores/test_N_iforest.csv`, `preprocessed/test/*.parquet`. 3.1의 효과 크기와 2.3의 구간별 표는 이 파일들에서 다시 계산할 수 있다. **주의**: 이 경로들은 6절 실행으로 덮어써졌다. 1~3절 시점의 지표는 `results/p0/0_baseline/`에, 절제 실험 A·C는 `results/p0/a_oldrule_excl_gate/`·`results/p0/c_newrule_plain/`에 있다(모델 파일은 지웠고 지표 JSON만).

### 6. P0-1 실행 결과 (2026-09-23, 비용접 구간 처리)

1~3절은 실행 전 상태(구 c16 규칙, 게이트 없음)의 진단이고 그대로 둔다. 아래가 실행 후 상태다.

#### 6.1 무엇을 했나

- **③ c16 확인 → 규칙 수정.** test_2는 행의 92%, test_3은 85%가 `c16 = 8.7`이다. 학습 파일에서 `c16 > 5`는 행의 1.3% 미만인데 그 행들은 **시간당 800~980회 용접**한다(E01_0·E02_1·E04_4 실측; 같은 파일의 `0 < c16 ≤ 5` 구간 520~600회보다 빠르다). 즉 `> 5`는 캡 드레싱이 아니라 **두꺼운 판 setpoint**이고, `> 5` 분기는 그것을 비용접으로 오분류했다. 학습 파일의 실제 비용접(카운터가 거의 안 도는 구간)은 사실상 전부 `c16 == 0`이다. 규칙을 `c16 <= 0`만으로 바꿨다(`--outlier-hi` 기본 `None`, `--outlier-hi 5`면 논문 규칙 복원). 전처리 2·3단계 재실행.
- **① 학습 제외.** `--exclude-non-welding`으로 비용접 비율 > 0 인 윈도우를 학습에서 뺀다(정상 윈도우 464,395 → 418,328).
- **② 알람 보류 게이트.** 비용접 비율이 `--alarm-max-non-welding`(기본 0.5)을 넘는 윈도우는 점수가 임계값을 넘어도 알람으로 세지 않는다. 평가(`train.py`)와 서빙(`main.py`)이 같은 규칙을 쓰고, 값은 번들 `alarm_max_non_welding`에 들어간다. API는 `alarm_held`·`hold_reason`을 돌려주며 보류 윈도우는 `consecutive_alarms`를 0으로 리셋한다.

#### 6.2 절제 실험 (테스트 8건, 같은 임계값 설계·같은 분할)

| 구성 | c16 규칙 | 학습 제외 | 게이트 | AUROC | recall@1h | 정상 알람률 | test_2 / test_3 정상 알람률 | 지속 오탐/일 test_2 / test_3 | 게이트 대상 윈도우 | E02 / E04 AUROC |
|---|---|---|---|---|---|---|---|---|---|---|
| 0. 이전 기준선 | `>5 or ≤0` | ✗ | ✗ | 0.687 | 0.054 | 3.2% | 11.0% / 13.2% | 8.3 / 7.2 | — | 0.800 / 0.678 |
| A. 규칙 유지 + ①② | `>5 or ≤0` | ✓ | 0.5 | 0.682 | 0.023 | 0.0% | 0.0% / 0.0% | 0 / 0 | **30%** (test_2·test_3은 99.8%) | 0.805 / 0.673 |
| C. 규칙 수정만 | `≤0` | ✗ | ✗ | 0.680 | 0.035 | 0.5% | 0.1% / 0.1% | 0 / 0 | 0% | 0.870 / 0.707 |
| **최종 (규칙 수정 + ①②)** | `≤0` | ✓ | 0.5 | 0.679 | 0.046 | **0.7%** | **0.7% / 0.3%** | **0 / 0** | 5.3% (244건 보류) | **0.864 / 0.690** |

읽는 법:

- A는 목표 수치(정상 알람률 0%)를 만들지만 **test_2·test_3 윈도우의 99.8%를 게이트로 가려서** 얻은 것이다. 그 두 건에서 모델은 아무것도 보지 않는다. 채택하지 않는다.
- 오탐의 원인은 게이트가 아니라 **규칙**이었다. C(규칙만 수정)에서 이미 test_2·test_3 정상 알람률이 0.1%로 떨어지고 E02 AUROC가 0.80→0.87로 오른다.
- 최종 구성은 C보다 recall@1h이 조금 높고(0.035→0.046) 정상 알람률은 조금 높다(0.5→0.7%). 차이는 학습 정상 윈도우가 바뀌어 임계값(0.5410)의 기준 분포가 달라진 데서 온다. ①②는 성능을 올리는 장치가 아니라 **"비용접 중에는 판단하지 않는다"는 운영 규칙**이고, 그 비용(보류 244건, 게이트 대상 5.3%)이 작아서 유지한다.

#### 6.3 최종 모델 파일별

| 파일 | 클래스 | 마지막 지속 알람 시작 (고장 전) | 지속 오탐 / 일 | 정상 알람률 (이전) | 고장 전 1h 알람률 | 비용접 비율 전체 / 고장 전 1h (이전 전체) |
|---|---|---|---|---|---|---|
| test_0 | E01 | 없음 | 0.00 | 1.6% (0.3%) | 0% | 12% / 0% (12%) |
| test_1 | E01 | 없음 | 0.00 | 1.7% (0.0%) | 2% | 2% / 2% (3%) |
| test_2 | E02 | 없음 | 0.00 (8.3) | 0.7% (11.0%) | 20% | 2% / 1% (**95%**) |
| test_3 | E04 | 없음 (이전 1.6분) | 0.00 (7.2) | 0.3% (13.2%) | 10% | 2% / 6% (**87%**) |
| test_4 | E03 | 없음 | 0.00 | 0.2% (0.0%) | 2% | 0% / 0% (0%) |
| test_5 | E03 | 없음 | 0.00 | 0.0% (0.0%) | 3% | 5% / 22% (5%) |
| test_6 | E04 | 없음 | 0.17 | 0.8% (0.1%) | 0% | 11% / 1% (12%) |
| test_7 | E04 | 없음 | 0.33 | 0.6% (0.0%) | 0% | 10% / 1% (10%) |

- **확인 지표 달성**: test_2·test_3 정상 알람률 1% 미만, 지속 오탐 0/일. E02 AUROC 0.80→0.86, E04 0.68→0.69.
- 1절 판정은 **여전히 불충분**이다. 고장으로 이어지는 지속 알람은 이제 **0/8건**(이전 1건도 test_3의 인공물이었다). 3.3에서 "E02·E04 지표는 상당 부분 인공물"이라 한 것과 달리 E02 AUROC는 오히려 올랐지만, 마지막 10분 알람률 50%·10분~1h 14%로 30분 선행 목표에는 못 미친다.
- 정상 알람률이 3.2%→0.7%로 내려간 대신 E01 두 건이 0→1.6%로 올랐다. 학습 정상 분포가 바뀌면서 건별 오프셋이 다른 자리에서 드러난 것이고, 이것이 P0 두 번째 항목(건별 정규화·임계값)의 대상이다.
- 점수 구간별(2.3 대응, 중앙값/알람률): E02 마지막 10분 0.542/50%, 10분~1h 0.486/14%, 1~6h 0.489/2.3%; E04 0.462/19%, 0.445/0%, 0.430/0.8%; E03 0.474/5%, 0.457/2%; E01은 여전히 평평(0.42~0.43, 0~1.6%).

#### 6.4 남은 것

- 새 규칙에서 학습 비용접 비율은 파일당 0.1~44.1%, 평균 10.1%(이전 11.3%). E02_x 44%짜리는 그대로다 — `c16 == 0` 구간이 실제로 긴 파일이며 캡 드레싱인지 비가동인지는 데이터로 구분할 수 없다.
- `c16 = 8.7`(두꺼운 판) 구간은 이제 z-score 입력으로 들어간다. test_2·test_3의 정상 알람률이 0.3~0.7%라 현재는 문제가 되지 않지만, 판 두께가 다른 새 건에서는 3.4의 오프셋 문제로 나타날 수 있다.
- **단, test_2·test_3의 `c16 = 8.7` 구간은 대부분 대기 상태다**: 용접 시간당 27회(test_2)·72회(test_3)로, 같은 파일의 얇은 판 구간(1,560~1,600회)의 2~5%이고 `c16 ≤ 0` 구간(17~49회)과 비슷하다. 규칙 수정은 "setpoint로 비용접을 판정하지 않는다"는 뜻이지 이 두 건이 활발히 용접 중이었다는 뜻이 아니다. 저활동은 `welds_10min`·`weld_duty_10min` 피처가 이미 표현하며, 두 건의 낮은 정상 알람률은 **대기 상태 데이터에서의** 결과다. 대기가 긴 건에서 모델이 무엇을 보는지는 3.4·P1의 문제로 남는다.
- 다음: P0 두 번째 항목(`--drop-static` + 건별 스케일·임계값). → 7절에서 실행.

### 7. P0-2 실행 결과 (2026-09-23, 건별 정규화·임계값)

#### 7.1 설계 — 서빙이 재현할 수 있는 건별 정규화

`preprocess.py --scale per-file`은 파일 전체의 통계를 쓰므로 온라인에서 재현할 수 없다(서빙이 거부함). 대신 **워밍업 방식**으로 갔다.

- **건별 통계**: 파일(스트림)의 **첫 6시간** 중 정상·용접 행(`non_welding==0 & error_active==0`, 600행 이상)의 평균을 구해, 6시간 **이후** 행의 z-score 입력 19개(`c1~c9, c13~c19, welds_delta, pos_delta, welds_10min`)에서 뺀다(`--gun-norm center`). 건 상수 c7~c9는 자연히 0이 되어 `--drop-static`과 같은 효과다. 워밍업 6시간 자체는 글로벌 스케일로 점수화하고 `warmup` 플래그를 단다 — 서빙이 그 시점에 가진 정보가 그것뿐이기 때문이다.
- **건별 임계값**: 워밍업 윈도우를 건별 통계로 다시 정규화해 점수화하고, 그 99% 분위수와 글로벌 임계값 중 **큰 쪽**을 그 건의 임계값으로 쓴다(기본 켜짐, `--no-gun-threshold`로 해제). 워밍업 6시간(≈360윈도우)의 99% 분위수는 거칠어서 "낮추는" 쪽으로는 쓰지 않는다.
- **학습·평가·서빙이 같은 절차**: `train.window_file()`이 오프라인, `main.Detector.gun_normalise()`가 온라인. 번들 `gun_norm`에 레시피가 들어가고, API는 `gun_norm`(warming_up/gun/global)·`gun_threshold`·(그 윈도우를 판정한) `threshold`를 돌려준다. `DELETE /guns/{id}`가 워밍업을 다시 시작한다(정비 후).
- 워밍업에 정상·용접 행이 600개 미만이면 그 건은 글로벌 스케일로 남는다(검증 E03_10이 해당).

#### 7.2 절제 실험 (모두 새 c16 규칙 + `--exclude-non-welding` + 게이트 0.5)

| 구성 | 글로벌 임계값 | 검증 AUROC / recall@1h / 정상 알람률 | 검증 건별 정상 알람률 min~max (sd) | 테스트 AUROC / recall@1h / 정상 알람률 | 테스트 건별 min~max (sd) | 1% 초과 건 (검증 / 테스트) |
|---|---|---|---|---|---|---|
| 이전 기준선 (P0-1 전) | 0.5411 | 0.630 / 0.044 / 1.4% | 0.0~8.6% (2.7%) | 0.687 / 0.054 / 3.2% | 0.0~13.2% (5.2%) | 3/14 / 2/8 |
| P0-1 후 | 0.5410 | 0.642 / 0.057 / 1.6% | 0.0~10.2% (2.6%) | 0.679 / 0.046 / 0.7% | 0.0~1.7% (0.6%) | 6/14 / 2/8 |
| E1 center | 0.5200 | 0.684 / 0.058 / 1.5% | 0.1~8.0% (2.1%) | 0.639 / 0.029 / 0.6% | 0.0~1.4% (0.4%) | 5/14 / 2/8 |
| **E2 center + 건별 임계값 (채택)** | 0.5200 | 0.684 / 0.039 / 1.1% | **0.1~4.9% (1.2%)** | 0.639 / 0.029 / 0.5% | **0.03~0.9% (0.3%)** | **4/14 / 0/8** |
| E3 scale (std 하한 0.25) | 0.5605 | 0.697 / 0.069 / 5.0% | 0.0~**56.5%** (14.4%) | 0.689 / 0.017 / 0.1% | 0.0~0.4% (0.1%) | 3/14 / 0/8 |
| E4 scale + 건별 임계값 | 0.5605 | E3과 동일 | E3과 동일 | E3과 동일 | E3과 동일 | 동일 |

읽는 법:

- **확인 지표(건별 정상 알람률 편차 → 1% 안팎)는 E2에서 달성**: 테스트 8건 전부 1% 미만(sd 0.3%), 검증 sd 1.2%(이전 2.6%). 검증에서 남은 4.9%는 E04_4 — 워밍업 6시간 동안 거의 대기 상태였던 건이다(아래).
- **`scale`은 폐기.** 테스트 수치는 가장 좋아 보이지만 검증 E04_4에서 정상 알람률 56.5%가 나온다. 워밍업 중 대기 상태라 std가 바닥(0.25)에 걸리고, 이후 실제 용접이 시작되면 모든 값이 수 σ로 튄다. E4에서 건별 임계값이 아무것도 못 바꾼 이유도 같다 — 워밍업 윈도우는 조용해서 임계값이 올라가지 않는다. **워밍업이 그 건의 정상 운전을 대표하지 않으면 std 추정은 위험하다.** 평균만 빼는 center가 안전한 쪽이다.
- 건별 임계값은 검증 E02_9(0.593)·E03_14(0.55), 테스트 test_0(0.577)·test_1(0.530)에서만 글로벌보다 올라갔고 나머지는 글로벌(0.520) 그대로다. 그 대가로 E02_9의 고장 전 지속 알람(9.8분 전)이 사라지고 검증 recall@1h이 5.8→3.9%로 내려갔다.

#### 7.3 최종 모델 (E2) 파일별

| 파일 | 클래스 | 건별 임계값 | 마지막 지속 알람 시작 (고장 전) | 지속 오탐 / 일 | 정상 알람률 (P0-1 후) | 고장 전 1h 알람률 |
|---|---|---|---|---|---|---|
| test_0 | E01 | 0.577 | 없음 | 0.00 | 0.5% (1.6%) | 0% |
| test_1 | E01 | 0.530 | 없음 | 0.00 | 0.8% (1.7%) | 0% |
| test_2 | E02 | 0.520 | 없음 | 0.33 | 0.8% (0.7%) | 10% |
| test_3 | E04 | 0.520 | 없음 | 0.17 | 0.3% (0.3%) | 7% |
| test_4 | E03 | 0.520 | 없음 | 0.00 | 0.1% (0.2%) | 0% |
| test_5 | E03 | 0.520 | 없음 | 0.00 | 0.03% (0.0%) | 5% |
| test_6 | E04 | 0.520 | 없음 | 0.00 | 0.9% (0.8%) | 2% |
| test_7 | E04 | 0.520 | 없음 | 0.17 | 0.6% (0.6%) | 0% |

검증 14건에서는 고장으로 이어지는 지속 알람이 E03_10·E04_8에서 5.1분 전에 생겼고(이전엔 E02_1·E02_9·E04_8), 테스트는 여전히 0/8이다.

#### 7.4 판정에 미치는 영향 — 솔직한 부분

- **테스트 AUROC는 0.679 → 0.639로 내려갔다**(E01 0.44→0.40, E04 0.69→0.635; E02·E03은 유지). 검증은 0.642→0.684로 올랐다. 해석: 이전 테스트 AUROC의 일부는 "고장 전 1시간"이 아니라 **그 건의 오프셋**이 만든 숫자였다. 오프셋을 빼면 그 부분이 사라진다. P0의 취지("인공물을 같이 최적화하지 않는다")에 맞는 결과이고, 이 0.639가 지금 피처·모델의 진짜 자리다.
- 점수 구간별(중앙값/알람률): E02 마지막 10분 0.484/30%, 10분~1h 0.427/6%; E03 0.488/0%, 0.394/3%; E04 0.405/13%, 0.394/0.7%; E01 평평(0.38, 0~0.7%). **마지막 10분 이전에는 어떤 클래스도 알람률이 6%를 넘지 않는다.** 1절 판정(불충분)은 그대로이고, 남은 것은 P1(변동성·추세 피처, 시간 축 모델, 지도학습 상한선)이다.
- 워밍업 6시간은 파일 길이(34.7~168시간)에 비해 짧지만, 새 건이 투입되면 처음 6시간은 글로벌 기준으로만 판정된다. (7절 시점의 48피처에는 c19가 들어 있었다 — 8.1 참고. 7절의 수치는 c19 포함 기준이다.) 워밍업이 대기 상태로 채워진 건(E04_4)은 그 후에도 글로벌과 비슷하게 남는다 — 워밍업 조건을 "6시간"이 아니라 "정상 용접 N행"으로 바꾸는 것이 다음 개선 후보다.
- 서빙 검증: test_0의 첫 6.5시간을 25분씩 보내면 워밍업 종료 후 API 점수가 오프라인과 같고(0.45415) 건별 임계값 차이는 0.0002(0.5770 vs 0.5772). **청크가 30분 버퍼보다 크면 워밍업 행이 유실**되므로(50분 청크: 21,600행 중 13,688행만 수집, 임계값 0.5767) 클라이언트는 30분보다 잘게 보내야 한다.

### 8. P1·P2 실행 결과 (2026-09-23)

실험은 `.py/experiments.py`(윈도우 캐시 → 실험 → `results/p1p2/<이름>.json`)로 돌렸다. 모든 실험은 P0 이후 레시피(새 c16 규칙, `--exclude-non-welding`, 게이트 0.5, 건별 centering + 건별 임계값)를 공통으로 쓴다. `base60`이 7절의 운영 모델과 정확히 같다(임계값 0.5200, AUROC 0.684/0.639).

#### 8.1 먼저 드러난 것 — c19는 레이블 누설이다

지도학습 상한선(P1 세 번째 항목)을 재자마자 검증 AUROC 0.952 / 테스트 **0.982**, recall@1h 86%, 8건 중 7건에서 중앙값 78분 전 지속 알람이 나왔다. 너무 좋았다. 이득의 69%가 `c19_mean` 하나였다.

`c19`("offset value in robot")는 **파일 안에서 초당 약 55씩 단조 증가하는 카운터**다(경과 시간과의 상관 1.000, 한 파일에서 9.4억 → 9.7억). 모든 파일이 정확히 7일이고 고장에서 끝나므로, 건별 centering 후 c19는 곧 "시작 후 경과 시간 = 168h − 잔여 시간"이다. 센서가 아니라 시계이고, 이 데이터셋 구조에서는 레이블 그 자체다. **c19를 빼면 지도학습도 검증 0.705 / 테스트 0.619로 내려와 비지도와 같은 자리에 선다.**

조치: `train.py`가 기본으로 `c19`를 모델 입력에서 뺀다(`--drop-features`, 번들 `dropped_features`, API `/model`에 노출). 운영 모델을 46피처로 재학습했다(8.5). 아래 표의 `noc19_*`가 c19만 뺀 것, `clean_*`은 시각(`hour_sin/cos`)과 건 상수(c7~c9)까지 뺀 것이다.

#### 8.2 실험 표

검증 14건 / 테스트 8건. "지속 알람 건"은 고장으로 이어지는 3연속 알람이 뜬 건 수(중앙값 선행 분), "운영점"은 선행 30분 이상 **그리고** 오탐 1회/일 이하를 만족한 건 수.

| 실험 | 피처 | 검증 AUROC | recall@1h | 정상 알람률 | 지속 알람 건 | 테스트 AUROC | recall@1h | 정상 알람률 | 지속 알람 건 | 운영점 (검증/테스트) |
|---|---|---|---|---|---|---|---|---|---|---|
| base60 (7절 모델) | 48 | 0.684 | 0.039 | 1.1% | 2/14 (5.1분) | 0.639 | 0.029 | 0.5% | 0/8 | 0 / 0 |
| **noc19_base60 (채택 → 8.5)** | 46 | 0.647 | 0.030 | 1.0% | 0/14 | 0.636 | 0.029 | 0.5% | 0/8 | 0 / 0 |
| clean_base60 | 36 | 0.656 | 0.061 | 2.2% | 3/14 (8.7분) | 0.630 | 0.037 | 0.5% | 0/8 | 0 / 0 |
| **P1-1 변동성·추세 피처** | | | | | | | | | | |
| hist_std60 (+5/30/60/1440창 이력·추세, `_std` 24개) | 144 | 0.702 | 0.040 | 0.4% | 3/14 (7.1분) | 0.611 | 0.027 | 0.3% | 1/8 (7.6분) | 0 / 0 |
| hist_all60 (48개 전부) | 240 | 0.684 | 0.046 | 0.4% | 3/14 | **0.524** | 0.015 | 0.1% | 0/8 | 0 / 0 |
| clean_hist_std60 | 108 | 0.686 | 0.074 | 0.8% | 4/14 (8.4분) | 0.640 | 0.035 | 0.3% | 1/8 (9.6분) | 0 / 0 |
| base300 (5분 창) | 48 | 0.689 | 0.084 | 1.7% | 2/14 (12.9분) | 0.636 | 0.030 | 0.7% | 0/8 | 0 / 0 |
| clean_base300 | 36 | 0.664 | 0.084 | 3.8% | 2/14 (11.1분) | 0.637 | 0.051 | 1.3% | 0/8 | 0 / 0 |
| hist_std300 | 144 | 0.673 | 0.012 | 0.2% | 0/14 | 0.643 | 0.010 | 0.1% | 0/8 | 0 / 0 |
| **P1-2 시간 축** | | | | | | | | | | |
| ewma5 (점수 EWMA, 반감기 5창) | 48 | 0.692 | 0.022 | 0.3% | 2/14 (8.4분) | 0.647 | 0.000 | 0.2% | 0/8 | 0 / 0 |
| ewma15 | 48 | 0.686 | 0.000 | 0.2% | 0/14 | 0.645 | 0.000 | 0.2% | 0/8 | 0 / 0 |
| clean_ewma5 | 36 | 0.661 | 0.077 | 2.1% | 6/14 (5.9분) | 0.637 | 0.012 | 0.0% | 1/8 (5.6분) | 0 / 0 |
| cusum (표준화 점수, k=0.5) | 48 | 0.547 | 0.138 | 2.2% | 2/14 (22.7분) | 0.440 | 0.000 | 0.0% | 0/8 | 1 / 0 |
| lstm_ae60 (30창 시퀀스 오토인코더) | 48 | 0.607 | 0.039 | 0.3% | 2/14 (15.9분) | 0.590 | 0.000 | 0.5% | 0/8 | 0 / 0 |
| **P1-3 지도학습 상한선 (LightGBM, `label`)** | | | | | | | | | | |
| sup_lgbm60 — **c19 누설** | 48 | 0.952 | 0.652 | 1.2% | 11/14 (34분) | 0.982 | 0.859 | 1.6% | 7/8 (78분) | 6 / 6 |
| sup_lgbm300 — 누설 | 48 | 0.947 | 0.620 | 0.9% | 11/14 (45분) | 0.969 | 0.747 | 1.2% | 6/8 (126분) | 8 / 5 |
| sup_lgbm_hist60 — 누설 | 240 | 0.955 | 0.643 | 2.6% | 12/14 (25분) | 0.960 | 0.768 | 4.3% | 7/8 (49분) | 6 / 4 |
| **noc19_sup60 (정직한 상한선)** | 46 | **0.705** | 0.183 | 1.4% | 9/14 (11.0분) | **0.619** | 0.180 | 1.1% | 5/8 (9.2분) | 0 / 0 |
| clean_sup60 | 36 | 0.648 | 0.140 | 0.1% | 7/14 (7.1분) | 0.589 | 0.112 | 0.1% | 4/8 (8.6분) | 0 / 0 |
| clean_sup_hist60 | 180 | 0.722 | 0.149 | 0.2% | 9/14 (9.1분) | 0.602 | 0.220 | 8.4% | 6/8 (9.2분) | 1 / 0 |
| clean_sup300 | 36 | 0.653 | 0.133 | 0.0% | 2/14 (30.5분) | 0.555 | 0.081 | 0.1% | 0/8 | 2 / 0 |
| **P1-4 클래스별 분리** | | | | | | | | | | |
| e02_only60 (E02 7건으로만 학습, E02만 평가) | 48 | 0.728 (E02) | 0.019 | 0.3% | 0/2 | 0.846 (test_2) | 0.000 | 0.0% | 0/1 | 0 / 0 |
| no_e02_60 (base60을 E02 제외 평가) | 48 | 0.679 | 0.032 | 1.0% | 2/12 | 0.638 | 0.019 | 0.5% | 0/7 | 0 / 0 |
| **P2-2 레이블 창 민감도 (IF / LightGBM, c19 제외)** | | | | | | | | | | |
| clean_labelwin_1800 (30분) | 36 | 0.713 / 0.672 | 0.100 / 0.175 | 2.2% / 0.3% | 2/14 / 6/14 | 0.673 / 0.717 | 0.074 / 0.190 | 0.5% / 0.1% | 0/8 / 4/8 (8.6분) | 0 / 0 |
| (3600, 1시간 = 기본) | 36 | 0.656 / 0.648 | 0.061 / 0.140 | | 3/14 / 7/14 | 0.630 / 0.589 | 0.037 / 0.112 | | 0/8 / 4/8 | 0 / 0 |
| clean_labelwin_7200 (2시간) | 36 | 0.621 / 0.598 | 0.042 / 0.062 | 1.7% / 0.0% | 2/14 / 4/14 | 0.623 / 0.514 | 0.024 / 0.062 | 0.4% / 0.1% | 1/8 / 6/8 (8.2분) | 0 / 0 |
| clean_labelwin_21600 (6시간) | 36 | 0.563 / 0.619 | 0.025 / 0.030 | 1.6% / 0.0% | 2/14 / 4/14 | 0.640 / 0.628 | 0.010 / 0.031 | 0.4% / 0.1% | 0/8 / 4/8 | 0 / 0 |
| **P2-2 건 단위 4-fold (64건 전부)** | | | | | | | | | | |
| cv4_base60 | 48 | 0.670 ± 0.044 (폴드 0.62~0.73) | 0.028 ± 0.013 | 1.4 ± 0.8% | 3/64 | 0.632 ± 0.023 | | | | 0 / 0 |
| clean_cv4 | 36 | 0.641 ± 0.040 (0.59~0.70) | 0.031 ± 0.008 | 1.5 ± 0.7% | 3/64 | 0.626 ± 0.023 | | | | 0 / 0 |
| **P2-1 종료 코드 규칙 (모델 무관 기준선)** | — | — | — | — | 14/14 (중앙값 9.3분) | — | — | — | 8/8 (9.1분) | 0 / 0 |

#### 8.3 항목별 판단

- **P1-1 변동성·추세 피처 — 채택 안 함.** 이력 피처는 검증 AUROC를 0.70까지 올리지만 테스트에서 떨어지고(hist_std60 0.611, hist_all60 0.524), c19를 뺀 clean 비교에서는 +0.03/+0.01로 4-fold 편차(±0.04) 안이다. "마지막 지속 알람이 10분 밖에서 시작하는 건"은 어느 구성에서도 0건. 5분 창(`--window 300`)은 검증 recall을 4→8%로 올리지만 AUROC·테스트는 같고 오탐이 는다. 온라인에 건별 윈도우 이력(최대 24시간)을 들고 다녀야 하는 비용을 정당화할 이득이 없다.
- **P1-2 시간 축 모델 — 채택 안 함.** 점수 EWMA는 AUROC +0.01, recall 0%(평활이 마지막 10분 급등을 지운다). CUSUM은 건별 점수 오프셋이 누적되어 임계값이 8,570으로 발산 — 건별 임계값 아래에서도 점수 평균이 건마다 달라 이 형태로는 쓸 수 없다. LSTM-AE(30창, 6 epoch, 정상 시퀀스 6만 개)는 0.607/0.590으로 IsolationForest보다 나쁘다. **시간 기억이 없어서가 아니라 기억할 신호가 없다.**
- **P1-3 지도학습 상한선 — 결론이 뒤집혔다.** 누설을 걷어낸 상한선은 검증 0.705 / 테스트 0.619이고, 비지도 0.647 / 0.636과 사실상 같다. 3.1에서 "피처 문제인지 모델 문제인지"를 물었는데 답은 **피처(정보) 문제**다: 이 48(46)개 윈도우 통계에는 고장 1시간 전을 가르는 정보가 없다. 정답을 알려줘도 못 배운다. 이력 피처를 더해도(clean_sup_hist60) 0.722/0.602다. 지도학습이 잡는 것은 마지막 10분의 종료 상태(`error_share_10min`이 이득 22%)뿐이고, 그것은 규칙이 공짜로 준다. 상한선 없이 모델을 계속 바꾸는 것은 의미가 없다 — 다음은 새 정보원(용접 단위 품질 신호, 정비 기록, 더 긴 창의 원시 파형)이다.
- **P1-4 클래스별 분리 — 채택 안 함.** E02 전용 모델은 E02 AUROC를 오히려 낮춘다(검증 0.78→0.73, test_2 0.86→0.85; E02 학습 건 7개로는 부족). E02를 빼도 전체 지표는 같다(0.679/0.638) — E02가 전체를 왜곡하지 않는다.
- **P2-1 종료 상태 규칙 — 구현.** `main.py`가 최신 error 코드가 종료 코드(E012/E016/E028/E029)면 `rule_triggered=true`, `severity=critical`로 강제한다(`severity_source`: model/rule/model+rule). `train.evaluate()`도 같은 규칙을 기준선으로 계산한다: 종료 상태 윈도우 알람률 100%(정의상), 에러 상태 전체 알람률은 0.8%→1.3%(종료 코드가 아닌 E003류는 여전히 모델만 본다), 규칙 선행 시간 중앙값 검증 9.3분 / 테스트 9.1분, 8/8건. **규칙은 정확히 "마지막 10분"을 보장하고 그 이상은 못 한다.**
- **P2-2 평가 설계 — 구현.** (a) 운영점(선행 ≥30분, 오탐 ≤1회/일/건)을 `evaluate()`가 세고 `train.py`가 출력한다. (b) `--cv k`(건 단위 층화 k-fold)가 `metrics["cv"]`에 폴드별·평균±편차를 남긴다. (c) `--label-window`로 레이블 창을 바꿔 평가한다. (d) 종료 코드 규칙이 모든 평가에 기준선으로 같이 나온다. **레이블 창 민감도**: 30분 창에서 AUROC가 가장 높고(0.713/0.673) 창을 넓힐수록 떨어진다 — 신호가 마지막 수십 분에만 있다는 3.1·2.3의 재확인. **4-fold**: 폴드 간 AUROC 0.59~0.73으로 한 분할의 수치(0.647)는 ±0.04는 흔들린다고 읽어야 한다.

#### 8.4 판정

**여전히 불충분하고, 이제는 이유가 분명하다.** 어떤 구성도 운영점(선행 30분·오탐 1회/일)을 만족한 테스트 건이 0이다. 유일하게 만족한 것은 c19 누설 모델뿐이고 그것은 시계를 읽은 것이다. 정직한 상한선(0.62~0.70)이 비지도 수준이므로 **이 피처 세트로는 모델을 무엇으로 바꿔도 10분을 넘기지 못한다.** 지금 파이프라인이 실제로 보장하는 것은 (1) 정상 구간 오탐 0.5%(건별 편차 없음), (2) 종료 코드 규칙에 의한 고장 직전 10분 critical, (3) 알 수 없는 정보의 상시 점수·기여도다.

다음 단계는 모델이 아니라 데이터다: 용접 단위 품질 신호(c11 증분마다의 파형 요약), 정비·캡 교체 이력, 또는 "고장 1시간 전"이라는 레이블 정의 자체의 재검토(30분 창이 더 잘 갈라진다).

#### 8.5 운영 모델 갱신 (c19 제거, 46피처)

`python .py/train.py --exclude-non-welding --cv 4` → `python .py/train.py --evaluate`. 글로벌 임계값 0.5251.

| | AUROC | recall@1h | 정상 알람률 | 건별 정상 알람률 | 지속 알람 건 | 규칙 선행 |
|---|---|---|---|---|---|---|
| 검증 14건 | 0.647 (7절 0.684) | 0.030 | 1.0% | 0.1~4.0% (sd 1.1%) | 0/14 | 14/14, 중앙값 9.3분 |
| 4-fold (64건) | 0.631 ± 0.038 | 0.024 ± 0.007 | 1.4 ± 0.7% | | 1/64 | |
| 테스트 8건 | 0.636 (7절 0.639) | 0.029 | 0.5% | 0.02~1.0% (sd 0.3%) | 0/8 | 8/8, 중앙값 9.1분 |

클래스별 테스트 AUROC: E01 0.346 / E02 0.894 / E03 0.683 / E04 0.644. 테스트는 0.639→0.636로 같고(E01 0.40→0.35, E02 0.86→0.89), 검증은 0.684→0.647(E02 0.78→0.74, E03 0.70→0.63)로 내려갔다 — c19의 단조 추세가 검증 건들의 마지막 1시간 점수를 올리고 있던 몫이며, 4-fold(0.631 ± 0.038)가 보여 주듯 분할 하나의 차이는 편차 안이다.

### 9. MVP 리뷰 반영 — 서빙 트리거·스트림 처리 (2026-09-24)

목표를 "작동하는 MVP"로 두고 파이프라인을 점검했다. 모델은 그대로(같은 인자로 재학습, 임계값 0.5251·AUROC 동일)이고 바뀐 것은 서빙 트리거와 규칙 평가다.

#### 9.1 종료 코드 규칙 — 평가와 서빙이 달랐다

- 평가(`evaluate()`)는 파일 **자기 클래스**의 종료 코드만 셌다(`terminal_code`). 서빙은 클래스를 모르므로 4개 코드 중 무엇이든, 그리고 코드가 **떠 있는 동안 매 요청** critical로 올렸다.
- 테스트 CSV에서 고장 1h 이전에 종료 코드가 떠 있던 시간: test_0 16.2h(E029), test_4 9.4h(E029), test_1·test_5 짧게 7회. EDA에서도 E029는 E01·E02·E03 건에 최장 24h 나타난다. 서빙이었다면 그 시간 내내 critical + `known_code_class_hint=E04`.
- 수정: 에피소드 시작에서 1회 발화, 건별 쿨다운 30분(`train.RULE_COOLDOWN_S`, 번들 `rule`). 평가는 윈도우 플래그 `terminal_any`(4개 코드)로 같은 규칙을 돌린다(`train.rule_triggers`).

| | 적중 | 선행 중앙값 | 오트리거/일/건 | 오트리거 있는 건 |
|---|---|---|---|---|
| 검증 14건 | 14/14 | 9.4분 | 0.05 | 3 |
| 테스트 8건 | 8/8 | 9.2분 | 0.25 (test_0 0.72, test_1 0.86, test_4 0.29, test_5 0.14) | 4 |

이전 표기(8/8건, 9.1분, test_3 6.6분)는 자기 클래스 코드만 센 수치였다. test_3은 E016이 먼저 떠서 이제 9.6분이고, 쿨다운이 코드를 가리지 않아 규칙 힌트는 E02가 된다(추론 클래스 E04).

#### 9.2 지속 알람이 요청 횟수 기준이었다

`consecutive_alarms`를 요청마다 올려 `sustain`(3)과 비교했다. 오프라인의 "3회 연속"은 겹치지 않는 60초 윈도우 3개 = 3분인데, 서빙은 1초마다 호출하면 3초, 25분 청크면 75분에 critical이 됐다. 수정: 알람 시작 시각을 기록해 `window_end − alarm_since ≥ sustain × window`(180초)일 때 지속 알람. 테스트는 10초 청크로 보내도 180초에 critical이 되는지 확인한다.

#### 9.3 청크 처리

- 요청은 3,600행까지 받는데 버퍼는 1,800행이라 넘는 행이 조용히 버려졌다 → 한도 1,200행(버퍼 − 롤링 이력 10분), 초과 시 422.
- 청크 하나에서 마지막 윈도우만 스코어링해 청크 중간의 알람이 보이지 않았다 → 직전 요청 이후 완결된 윈도우를 최신 행부터 60초 간격으로 모두 스코어링하고 알람 상태를 윈도우마다 갱신. 응답은 최신 윈도우 + `windows_scored`, `sustained_in_request`, `critical_in_request`.

#### 9.4 실데이터 재생에서 드러난 것 (`.py/replay.py`)

- **HTTP 500**: test_0에서 30분 넘는 비용접 구간(c16 = 0)이 버퍼 전체를 채우자, 버퍼 안에서만 ffill하던 온라인 전처리가 carry할 용접 값을 잃고 NaN을 냈다(수정 전부터 있던 결함). 건별로 마지막 용접 값을 들고 있다가 채우도록 수정(오프라인의 무제한 carry와 같음).
- **규칙 힌트 소실**: 규칙이 발화한 청크의 끝에서는 코드가 이미 `0`이라 `known_code_class_hint`가 None이었다 → `rule_code`/`rule_class_hint` 추가.
- 결과: 테스트 8건 × 마지막 12h(10분 청크) 83초에 오류 없이 재생, 8/8건 고장 직전 규칙 critical, test_2·test_3은 모델 지속 알람도 함께. 7일 전체(20분 청크)는 건당 ~100초, test_0 규칙 발화 6회(E029 오트리거 5회 + 고장 직전 E012 1회).

### 10. ML → RAG 핸드오프 v1.0 (2026-09-25)

**결정.** 온톨로지(원인·점검·매뉴얼)는 지현, RAG 리포트는 RAG 담당이 만든다. ML은 ① 이상 감지와 ② 증상 번역까지만 하고, 두 쪽을 **상황 ID(S01~S10, 「RSW 용접건 MVP 오류 상황 정의서」)**로 잇는다. 첫 초안은 부품·점검 순서·매뉴얼 쪽수까지 넣었으나(③ 침범), 온톨로지와 내용이 겹치고 어긋날 수 있어 같은 날 뺐다.

**구현.** `.py/rag_mapping.py`(순수 Python: 기여 피처 → 방향 → 증상 P1~P9 → 고장 유형 → 상황 ID, 한 줄 요약), `main.py`(`RagHandoff` 스키마, `/predict`의 `handoff`, outbox + `GET /handoffs`, `RSW_RAG_URL` push, `POST /handoffs/preview`), `.py/mock_rag.py`(응답 계약을 보여 주는 가짜 RAG), `replay.py` 이벤트에 `situation_ids`·`summary_ko`. 트리거는 `replay.py`의 critical 이벤트 규칙과 같다.

**데이터 점검 1 — 증상 규칙을 데이터로 정당화할 수 있나.** 학습 20건(클래스당 5건, E02_2 제외)에서 용접 행(c16 > 0)만 골라, 고장 전 60~10분과 10~0분 구간의 c1~c7 평균·표준편차를 첫 6시간 대비(평균 차 / 기준 std, std 비)로 비교했다(중앙값).

| 클래스 | 눈에 띄는 변화 (60~10분 전) | 비고 |
|---|---|---|
| E01 | c2 std 비 36.7, c5 std 비 6.1, c2 평균 +10.7σ | 유효 3건뿐, 건 간 편차 큼 |
| E02 | c6 평균 −1.7σ, 나머지 ±0.2σ 안 | 거의 변화 없음 |
| E03 | c7 평균 −2.0σ, c5 −0.65σ | |
| E04 | c6 평균 +0.6σ(10~0분 +1.5σ), c1 +0.7σ | |

방향이 매뉴얼 규칙(예: E01 = c5 평균 low)과 맞지 않거나 건 수가 적다 → 증상은 조회 후보, 신뢰도 상한 medium(규칙 발화 + 유형 일치일 때만).

**데이터 점검 2 — 실서빙 경로로 재생.** `main.predict`를 그대로 호출해(작업 환경에 fastapi·pydantic이 없어 최소 대역 모듈로 대체) 테스트 8건의 마지막 12시간을 10분 청크로 재생했다.

| 파일 (클래스) | 이벤트 | 상황 ID | 센서 증상 |
|---|---|---|---|
| test_0 (E01) | 2 | S04 (고장 7h 전 E029 오트리거), S01 | c8·c10 / 없음 |
| test_1 (E01) | 1 | S01 | 없음 |
| test_2 (E02) | 1 | S02, S10 | c16 흔들림(설정값) |
| test_3 (E02) | 1 | S02, S08, S03, S10 | c14·c15 설정값, P5 partial |
| test_4 (E03) | 1 | S03 | 없음 |
| test_5 (E03) | 1 | S03 | c5 high, c10 꺼짐 |
| test_6, test_7 (E04) | 1씩 | S04 | 없음(처음엔 시각 피처 → 제외) |

8/8건이 고장 직전 올바른 상황을 맨 앞에 냈다. 센서 증상이 매뉴얼 패턴(P1~P6)과 맞은 건 1건뿐 — 믿을 키는 고장 유형에서 온 `situation_ids[0]`이다. 이 결과로 세 가지를 고쳤다: 시각 피처(`hour_sin/cos`)를 증상에서 제외, c10을 "꺼짐(평소 켜짐)"으로 표기, 규칙 발화 + 센서 무증상일 때 S09(주변 설비)를 붙이지 않음(무증상은 "모델에 신호가 없다"는 뜻이지 "건이 정상"이 아니다, 8절). `docs/*.example.json`은 이 재생의 test_3 이벤트다.

**검증.** `tests/test_rag_mapping.py` 11개 통과(순수 Python). `tests/test_pipeline.py`에 핸드오프 단언 추가(규칙 발화 시 `handoff`·`fault_class=E04`·`situation_ids[0]=S04`·원인 필드 없음·outbox·404·preview, replay 이벤트마다 `handoff_event_id`) — **작업 환경에 fastapi가 없어 pytest로는 미실행, 로컬 `pytest` 필요.**

**미결.** readme 5.10.8.
